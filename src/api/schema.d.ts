/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/v1/healthz": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Liveness/readiness probe.
         * @description Reports whether the Control Plane is able to serve requests. Intended for
         *     load balancers and orchestrators. Public and unauthenticated; it MUST NOT
         *     disclose any identity, node, or policy information.
         */
        get: operations["getHealth"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/version": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Component and protocol version metadata.
         * @description Returns the Control Plane component name, its build version (SemVer), and
         *     the protocol version ranges it speaks on the CP<->Gateway gRPC plane and
         *     the Agent<->Gateway wire protocol. Public and unauthenticated.
         */
        get: operations["getVersion"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/bootstrap/claim": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Claim the printed-once first-admin bootstrap credential.
         * @description Surrenders the credential printed once to the CP's own log at cold
         *     start, binding `subject` as the initial `platform-admin` (Design §2A,
         *     FR-BOOT-2). Public and unauthenticated by scheme — the credential
         *     itself is the authenticator, matching `POST /v1/auth/device/poll`'s
         *     opaque-device-code model. Single-use: the underlying
         *     `operator_settings.bootstrap_completed` flip is a single
         *     conditional-UPDATE row lock, so a concurrent second claim (whether or
         *     not it knows the correct credential) always observes
         *     `already_completed`, never a second provision. Self-disables
         *     entirely once any platform admin exists (a config-named admin
         *     subject, or a prior successful claim) — no credential is ever armed
         *     again, so this operation can succeed at most once per deployment.
         *     Every attempt is audited, including invalid-credential attempts.
         */
        post: operations["claimBootstrap"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/oauth2/token": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * OAuth 2.0 client-credentials token endpoint (machine identity).
         * @description Machine consumers exchange a client credential — a `private_key_jwt`
         *     client assertion (RFC 7523) or a mutual-TLS client certificate,
         *     preferred over a static secret — for a short-lived CP-signed bearer
         *     token (Design §5.6, FR-AUTH-12). The token resolves to a first-class
         *     RBAC principal. The client authenticates itself here, so the endpoint is
         *     not behind the bearer/mTLS API gate; the `clientCredentials`/`mtls`
         *     schemes below document the accepted client-authentication methods.
         */
        post: operations["issueMachineToken"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/otp": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Issue a single-use OTP for an identity (admin).
         * @description Server-generates a single-use, short-TTL OTP bound to an identity and
         *     allowed principals (Design §5.4, FR-AUTH-9). The raw OTP is returned once
         *     (for out-of-band delivery); only its hash is stored. Platform-RBAC gated
         *     (`user:manage`) and audited.
         */
        post: operations["issueOtp"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/pins": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** List pins for an identity (admin). */
        get: operations["listPins"];
        put?: never;
        /**
         * Create an authN-shortcut pin (admin).
         * @description Pins a public-key fingerprint to `{identity, source-cidr, principals}`
         *     with a TTL capped at the authorization TTL (Design §5.5, FR-AUTH-10).
         *     Source IP is a deny-only reducer. Platform-RBAC gated + audited.
         */
        post: operations["createPin"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/pins/{pinId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Revoke a pin (admin / offboarding / lock). */
        delete: operations["revokePin"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/service-accounts/{serviceAccountId}/credentials": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Issue a machine-consumer credential (admin).
         * @description Issues a rotatable, revocable credential for a service account
         *     (FR-AUTH-12): a `private_key_jwt` public-key/JWKS reference, an mTLS
         *     certificate fingerprint, or (discouraged) a generated `client_secret`
         *     returned once. Stored hashed / by reference. Platform-RBAC gated +
         *     audited.
         */
        post: operations["issueServiceAccountCredential"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/service-accounts/{serviceAccountId}/credentials/{credentialId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Revoke a machine-consumer credential (admin).
         * @description Revocation takes effect immediately (new sessions denied, FR-AUTH-12).
         */
        delete: operations["revokeServiceAccountCredential"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/device": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Begin an OIDC device-authorization flow (RFC 8628).
         * @description Starts a device flow bound to the SSH source context (Design §5.2,
         *     FR-AUTH-3). Returns the user code + CP verification URI to present to the
         *     user, and the device code the caller polls with. Fallback-only. The
         *     caller is the Gateway (mTLS).
         */
        post: operations["beginDeviceFlow"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/device/poll": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Poll a device flow for completion (RFC 8628).
         * @description Polls with the opaque device code (its own credential). Returns
         *     `pending` until the user approves at the verification page, then
         *     `authorized` with the resolved identity, or `denied`/`expired`. Public
         *     (the device code is the authenticator); rate-limited.
         */
        post: operations["pollDeviceFlow"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/locks": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List active locks (admin).
         * @description Lists the current unexpired locks. Platform-RBAC gated (`lock:read`) +
         *     audited (Design §8.3; FR-LOCK-1). A lock is the incident-response deny
         *     primitive; the deny reason is operator-only.
         */
        get: operations["listLocks"];
        put?: never;
        /**
         * Create a lock (admin).
         * @description Creates an incident-response lock that blocks new session issuance AND
         *     tears down existing matching sessions on every Gateway (Design §8.3,
         *     §8.4; FR-LOCK-1/2), pushed over the actively-pushed deny-list. Ingest
         *     validation rejects an empty/unrecognised target; a fleet-wide lock
         *     requires an explicit `all: true`. Platform-RBAC gated (`lock:write`) +
         *     audited.
         */
        post: operations["createLock"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/locks/{lockId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Release a lock (admin).
         * @description Releases (deletes) a lock; the removal is pushed to every Gateway.
         *     Releasing a lock only stops future denial — it never resurrects a
         *     torn-down session. Platform-RBAC gated (`lock:write`) + audited.
         */
        delete: operations["releaseLock"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/join-tokens": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List active join tokens (admin).
         * @description Lists the current unconsumed, unexpired join tokens. The raw token value
         *     is NEVER returned (only its metadata) — it is shown exactly once at
         *     issuance. Platform-RBAC gated (`node:enroll`) + audited (Design §8.1;
         *     FR-JOIN-2).
         */
        get: operations["listJoinTokens"];
        put?: never;
        /**
         * Issue an agent join token (admin).
         * @description Issues a short-lived, single-use, self-destruct join token bound to a
         *     stable node identity/scope (Design §8.1; FR-JOIN-2/6). The raw token is
         *     returned exactly ONCE (for out-of-band delivery to the joining Agent);
         *     only its SHA-256 hash is stored. Because issuance is a pure API
         *     operation, an autoscaler/config-mgmt re-provisions a token-join agent
         *     after a full lapse without a human. Platform-RBAC gated (`node:enroll`)
         *     + audited.
         */
        post: operations["issueJoinToken"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/join-tokens/{joinTokenId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Revoke a join token (admin).
         * @description Revokes (deletes) an unconsumed join token so it can never be used.
         *     Idempotent. Revoking an already-consumed token has no effect on the
         *     identity it produced (revocation of an issued identity is a Lock, §8.1).
         *     Platform-RBAC gated (`node:enroll`) + audited.
         */
        delete: operations["revokeJoinToken"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/nodes": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List nodes (admin).
         * @description Lists enrolled nodes with their connectivity model, lifecycle status, and
         *     health (Design §12A; FR-NODE-1/2). Removed nodes are excluded by default.
         *     Platform-RBAC gated (`node:enroll`) + audited.
         */
        get: operations["listNodes"];
        put?: never;
        /**
         * Register an agentless node (admin).
         * @description Enrolls an AGENTLESS node (Design §9.2/§9.3; FR-NODE-1): registers its dial
         *     address and host-identity material — a host-CA-signed host certificate OR
         *     an explicitly pinned host key (at least one; NEVER TOFU) — with no software
         *     install. An agent node instead joins via a join token
         *     (`POST /v1/join-tokens`). Enrollment MAY require approval (operator
         *     setting): when on, the node starts `pending` and is excluded from targeting
         *     until activated. Provisioning is a pure API flow (closes the dev-seed
         *     gap). Platform-RBAC gated (`node:enroll`) + audited.
         */
        post: operations["registerNode"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/nodes/{nodeId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get a node (admin).
         * @description Returns one node by id. Platform-RBAC gated (`node:enroll`) + audited.
         */
        get: operations["getNode"];
        put?: never;
        post?: never;
        /**
         * Remove a node (admin).
         * @description Deregisters the node (soft-remove: status `removed`, excluded from
         *     targeting; session/audit history preserved) and, for an AGENT node, REVOKES
         *     its agent credential — the identity is flipped off `active` and a covering
         *     Lock is pushed, so the generation counter + lock make a stale clone
         *     unusable and re-join cannot bypass the revocation (FR-NODE-3). Idempotent.
         *     Platform-RBAC gated (`node:remove`) + audited.
         */
        delete: operations["removeNode"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/nodes/{nodeId}/quarantine": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Quarantine a node (admin).
         * @description Immediately blocks NEW sessions to the node and tears down or drains
         *     EXISTING ones (FR-NODE-3), expressed as a top-tier Lock on the node
         *     (deny wins) actively pushed to every Gateway — fail-closed. The
         *     `existingSessions` policy selects `kill` (default, torn down at once) vs
         *     `drain` (finish, no new channels). Platform-RBAC gated (`node:quarantine`)
         *     + audited.
         */
        post: operations["quarantineNode"];
        /**
         * Release a node from quarantine (admin).
         * @description Lifts a quarantine: releases the node Lock (pushed to every Gateway) and
         *     returns the node to `active`. Never resurrects a torn-down session.
         *     Idempotent. Platform-RBAC gated (`node:quarantine`) + audited.
         */
        delete: operations["releaseQuarantine"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/jit-requests": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List JIT requests (admin).
         * @description Lists JIT requests, optionally filtered by `state` or `requester` (Design
         *     §7; FR-ACC-2). Platform-RBAC gated (`request:approve`) + audited.
         */
        get: operations["listJitRequests"];
        put?: never;
        /**
         * Submit a JIT access request.
         * @description Requests just-in-time access to a target node as a principal for a bounded
         *     duration (Design §7; FR-ACC-2/3). Open to any authenticated principal — the
         *     requester is recorded from the authenticated caller, never a body field. The
         *     matching JIT policy's approval chain is snapshotted; a zero-level chain
         *     auto-approves (but a Lock still denies on use). Audited.
         */
        post: operations["submitJitRequest"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/jit-requests/{jitRequestId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Get a JIT request (admin). */
        get: operations["getJitRequest"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/jit-requests/{jitRequestId}/approve": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Approve a JIT request level (admin).
         * @description Approves the next level of a pending JIT request (Design §7; FR-ACC-3/4).
         *     The approver can NEVER be the requester (self-approval impossible) and may
         *     act at most once; when all levels are approved the grant clock starts.
         *     Platform-RBAC gated (`request:approve`) + audited.
         */
        post: operations["approveJitRequest"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/jit-requests/{jitRequestId}/deny": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Deny a JIT request (admin).
         * @description Denies a pending JIT request (terminal). The denier can never be the
         *     requester. Platform-RBAC gated (`request:approve`) + audited.
         */
        post: operations["denyJitRequest"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/jit-requests/{jitRequestId}/revoke": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Revoke an active JIT grant (admin).
         * @description Revokes an approved/active JIT grant (Design §7; FR-ACC-2). Also writes a
         *     strict Lock on the grant's identity+node so a LIVE session tears down
         *     (§8.4). Platform-RBAC gated (`request:approve`) + audited.
         */
        post: operations["revokeJitRequest"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/breakglass/credentials": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List break-glass credentials (admin).
         * @description Lists registered break-glass FIDO2 credentials (public metadata only)
         *     (Design §7; FR-ACC-6). Platform-RBAC gated (`breakglass:manage`) + audited.
         */
        get: operations["listBreakglassCredentials"];
        put?: never;
        /**
         * Register a break-glass FIDO2 key (admin).
         * @description Registers a break-glass FIDO2 `sk-ecdsa` PUBLIC key (Design §7; FR-ACC-6).
         *     Attestation is not required — an admin vouches for the key; only PUBLIC
         *     material and the SHA-256 fingerprint are stored. Platform-RBAC gated
         *     (`breakglass:manage`) + audited.
         */
        post: operations["registerBreakglassCredential"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/breakglass/credentials/{credentialId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Revoke a break-glass credential (admin).
         * @description Revokes (soft, `revoked_at`) a registered break-glass credential.
         *     Idempotent. Platform-RBAC gated (`breakglass:manage`) + audited.
         */
        delete: operations["revokeBreakglassCredential"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/breakglass/offline-codes": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List break-glass offline codes (admin).
         * @description Lists issued offline-code metadata (NEVER the raw codes) (Design §7;
         *     FR-ACC-6). Platform-RBAC gated (`breakglass:manage`) + audited.
         */
        get: operations["listBreakglassOfflineCodes"];
        put?: never;
        /**
         * Issue a batch of break-glass offline codes (admin).
         * @description Issues a batch of single-use break-glass offline codes (Design §7;
         *     FR-ACC-6). The raw codes are returned exactly ONCE; only their SHA-256
         *     hashes are stored. Platform-RBAC gated (`breakglass:manage`) + audited.
         */
        post: operations["issueBreakglassOfflineCodes"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/breakglass/activations": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List break-glass activations (admin).
         * @description Lists break-glass activations, optionally filtered by `reviewStatus`
         *     (Design §7; FR-ACC-6/FR-AUD-7). An unreviewed activation is a standing
         *     signal. Platform-RBAC gated (`breakglass:manage`) + audited.
         */
        get: operations["listBreakglassActivations"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/breakglass/activations/{activationId}/review": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Record the mandatory review of a break-glass activation (admin).
         * @description Records the mandatory post-hoc review of a break-glass activation (Design
         *     §7; FR-ACC-6/FR-AUD-7). Platform-RBAC gated (`breakglass:manage`) +
         *     audited.
         */
        post: operations["reviewBreakglassActivation"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/rules": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List data-plane rules (admin).
         * @description Lists data-plane RBAC rules (`config.dp_rule`, Design §6.1; FR-AUTHZ-1) —
         *     the typed allow/deny grants the engine evaluates. A committed deny that
         *     must persist is a rule; "deny now and keep it" is a runtime `Lock`, not a
         *     rule (Design §13). Cursor-paginated. Platform-RBAC gated (`rbac:read`).
         */
        get: operations["listRules"];
        put?: never;
        /**
         * Create a data-plane rule (admin).
         * @description Creates a data-plane rule. Invalid config (empty principals, ttl <= 0, an
         *     unknown capability, a malformed selector, effect not allow|deny) is
         *     rejected PRE-COMMIT (`422`, FR-API-5). The `origin` is server-set to `api`.
         *     Platform-RBAC gated (`rbac:write`) + audited.
         */
        post: operations["createRule"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/rules/{ruleId}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                ruleId: components["parameters"]["RuleId"];
            };
            cookie?: never;
        };
        /**
         * Get a data-plane rule (admin).
         * @description Returns one rule by id. Platform-RBAC gated (`rbac:read`).
         */
        get: operations["getRule"];
        /**
         * Update a data-plane rule (admin).
         * @description Replaces the mutable fields of a rule (the `name` is immutable). Invalid
         *     config is rejected pre-commit (`422`). An optional `version` enforces
         *     optimistic concurrency (a stale version is a `409`). Platform-RBAC gated
         *     (`rbac:write`) + audited.
         */
        put: operations["updateRule"];
        post?: never;
        /**
         * Delete a data-plane rule (admin).
         * @description Deletes a rule (idempotent — `204` whether or not it existed).
         *     Platform-RBAC gated (`rbac:write`) + audited.
         */
        delete: operations["deleteRule"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/roles": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List platform roles (admin).
         * @description Lists platform-RBAC roles (`config.platform_role`; FR-PADM-1) — named sets
         *     of the closed platform-permission vocabulary. Cursor-paginated.
         *     Platform-RBAC gated (`rbac:read`).
         */
        get: operations["listRoles"];
        put?: never;
        /**
         * Create a platform role (admin).
         * @description Creates a platform role. A permission outside the closed vocabulary is
         *     rejected pre-commit (`422`). Platform-RBAC gated (`rbac:write`) + audited.
         */
        post: operations["createRole"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/roles/{roleId}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                roleId: components["parameters"]["RoleId"];
            };
            cookie?: never;
        };
        /**
         * Get a platform role (admin).
         * @description Returns one role by id. Platform-RBAC gated (`rbac:read`).
         */
        get: operations["getRole"];
        /**
         * Update a platform role (admin).
         * @description Replaces a role's permissions/description (the `name` is immutable). An
         *     out-of-vocabulary permission is a `422`; a stale `version` is a `409`.
         *     Platform-RBAC gated (`rbac:write`) + audited.
         */
        put: operations["updateRole"];
        post?: never;
        /**
         * Delete a platform role (admin).
         * @description Deletes a role and cascades its bindings (idempotent). Platform-RBAC gated
         *     (`rbac:write`) + audited.
         */
        delete: operations["deleteRole"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/role-bindings": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List role bindings (admin).
         * @description Lists platform role bindings (`config.role_binding`; FR-PADM-2) — subject
         *     (user|group) -> role, optionally scoped for `recording:replay/export`.
         *     Cursor-paginated. Platform-RBAC gated (`rbac:read`).
         */
        get: operations["listRoleBindings"];
        put?: never;
        /**
         * Create a role binding (admin).
         * @description Binds a subject to a role. An unknown `roleId` or a malformed scope is a
         *     `422`; a duplicate (role, subjectKind, subject) is a `409`. Platform-RBAC
         *     gated (`rbac:write`) + audited.
         */
        post: operations["createRoleBinding"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/role-bindings/{bindingId}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                bindingId: components["parameters"]["BindingId"];
            };
            cookie?: never;
        };
        /**
         * Get a role binding (admin).
         * @description Returns one role binding by id. Platform-RBAC gated (`rbac:read`).
         */
        get: operations["getRoleBinding"];
        /**
         * Update a role binding (admin).
         * @description Replaces a binding's scope (the subject and role are immutable — rebind by
         *     delete + create). A stale `version` is a `409`. Platform-RBAC gated
         *     (`rbac:write`) + audited.
         */
        put: operations["updateRoleBinding"];
        post?: never;
        /**
         * Delete a role binding (admin).
         * @description Deletes a role binding (idempotent). Platform-RBAC gated (`rbac:write`) + audited.
         */
        delete: operations["deleteRoleBinding"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/cas": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List certificate authorities (admin).
         * @description Lists CA configurations (`config.ca_config`; FR-CA-1/4/7) — per-CA
         *     (user|session|host) backend + key REFERENCE and rotation state. NEVER
         *     exposes private key material (Design §13). Cursor-paginated. Platform-RBAC
         *     gated (`ca:manage`).
         */
        get: operations["listCas"];
        put?: never;
        /**
         * Create a CA configuration (admin).
         * @description Registers a CA configuration. An algorithm/backend mismatch (e.g.
         *     `ed25519` on `azure_keyvault`, which has no Ed25519 — Design D6) or a
         *     `keyReference` that looks like private material is rejected pre-commit
         *     (`422`, FR-API-5). The response NEVER contains private key material.
         *     Platform-RBAC gated (`ca:manage`) + audited.
         *
         *     **This is not how you adopt a new backend.** Cold start provisions an
         *     active CA of each kind and only one active CA per kind is permitted, so on
         *     any Control Plane that has booted, a create for an existing kind is a `409`
         *     naming `POST /v1/cas/{caId}/rotate` — which is the path that both installs
         *     the new key and keeps the old one trusted while the fleet catches up. This
         *     operation remains for a kind that has no active CA at all.
         */
        post: operations["createCa"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/cas/{caId}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                caId: components["parameters"]["CaId"];
            };
            cookie?: never;
        };
        /**
         * Get a CA configuration (admin).
         * @description Returns one CA configuration by id (public/config only, NEVER private
         *     material). Platform-RBAC gated (`ca:manage`).
         */
        get: operations["getCa"];
        /**
         * Update a CA configuration (admin).
         * @description Updates a CA configuration's backend/keyReference/algorithm (the `name` and
         *     `caKind` are immutable; `rotationState` is managed by the rotation state
         *     machine, not editable here — use rotate). An algorithm/backend mismatch is
         *     a `422`; a stale `version` is a `409`. Platform-RBAC gated (`ca:manage`) +
         *     audited.
         */
        put: operations["updateCa"];
        post?: never;
        /**
         * Delete a CA configuration (admin).
         * @description Deletes a CA configuration (idempotent). Deleting the sole `active` CA of a
         *     kind is rejected (`409`) — a kind must always retain a signer. Platform-RBAC
         *     gated (`ca:manage`) + audited.
         */
        delete: operations["deleteCa"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/cas/{caId}/rotate": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                caId: components["parameters"]["CaId"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Rotate a CA (admin).
         * @description Triggers key rotation for the CA kind of this configuration (FR-CA-7): a new
         *     key is provisioned and becomes `incoming`, the current `active` row moves to
         *     `outgoing` (both trusted during the overlap window), and the new key is
         *     promoted to `active`. The outgoing key still verifies existing certs until it
         *     expires. NEVER returns private material. Platform-RBAC gated (`ca:rotate`) +
         *     audited.
         *
         *     The incoming key is provisioned in the active CA's backend unless the body
         *     overrides `backend`, which is how a CA is **adopted onto a key service**: for
         *     `azure_keyvault` the key is created by the operator in the vault beforehand
         *     and named here by its versioned identifier, and the Control Plane reads only
         *     its public half. Nothing about the private key crosses this API in either
         *     direction.
         */
        post: operations["rotateCa"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/cas/mtls/trust-anchor": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Export the internal mTLS CA trust anchor (admin).
         * @description Returns the PEM-encoded X.509 **certificate** of the active internal mTLS
         *     CA — the trust anchor a Gateway pins to verify the Control Plane on the
         *     CP↔Gateway plane (Design §2A, §15; FR-CA-3). This is public material: it
         *     reads `runtime.ca_key_material.ca_certificate` and nothing else, and the
         *     wrapped private key is never touched, unwrapped, or returned.
         *
         *     The internal mTLS CA is deliberately absent from the `/v1/cas` collection
         *     (which serves the three SSH CA kinds), so it has no `caId` to address and
         *     no update/delete/rotate surface. This is a read-only sibling path, not a
         *     member of that collection.
         *
         *     Platform-RBAC gated (`gateway:enroll`) so an operator can bring up a
         *     Gateway without holding `ca:manage`.
         */
        get: operations["getMtlsTrustAnchor"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/cas/{caKind}/public-key": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Which SSH CA to address. The closed set is the three SSH kinds; the internal mTLS CA is not a member and any other value is a `400`. */
                caKind: components["parameters"]["CaKindPath"];
            };
            cookie?: never;
        };
        /**
         * Export an SSH CA's public key (admin).
         * @description Returns the PUBLIC key of the active CA of one SSH kind, in the two forms
         *     an operator actually needs: the DER SubjectPublicKeyInfo (base64) and the
         *     OpenSSH one-line authorized-key form. Installing a node requires the
         *     session CA's public key in `TrustedUserCAKeys` — without an export path
         *     the only source is a `SELECT` against `runtime.ca_key_material` as the
         *     database owner, on the path every node install must walk.
         *
         *     Public verification material only. The projection is column-scoped: it
         *     reads the stored public key and never touches the wrapped private key or
         *     routes through the backend unwrap path.
         *
         *     `caKind` is one of the three SSH kinds (`user`, `session`, `host`). The
         *     internal mTLS CA is not addressable here — it is not a member of this
         *     collection and its trust anchor has its own sibling path — so `mtls` is
         *     not an accepted value (`400`). Platform-RBAC gated (`node:enroll`), the
         *     permission installing a node already requires.
         *
         *     If the active CA of that kind carries an algorithm that has no OpenSSH
         *     key type — a row predating the create-time check, or one written directly
         *     to the database — the export REFUSES with `409` rather than emitting a
         *     line it cannot honestly label. Guessing would put a wrong
         *     `ecdsa-sha2-nistp256` line into `TrustedUserCAKeys` on every node, and
         *     that failure surfaces at session time, everywhere at once, with nothing
         *     pointing back to the export. `404` would be untrue, since the CA exists,
         *     and `400` would blame a caller who did nothing wrong.
         */
        get: operations["getCaPublicKey"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/gateway-enrollment-tokens": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List active Gateway enrollment tokens (admin).
         * @description Lists the current unconsumed, unexpired Gateway enrollment tokens. The raw
         *     token value is NEVER returned (only its metadata) — it is shown exactly
         *     once at issuance. Platform-RBAC gated (`gateway:enroll`) + audited
         *     (Design §4.B; FR-JOIN-3).
         */
        get: operations["listGatewayEnrollmentTokens"];
        put?: never;
        /**
         * Issue a Gateway enrollment token (admin).
         * @description Issues a short-lived, single-use enrollment token bound to one stable
         *     Gateway name (Design §4.B; FR-JOIN-3). The raw token is returned exactly
         *     ONCE, for out-of-band delivery to the Gateway being installed; only its
         *     SHA-256 hash is stored. It authorises nothing beyond the enrollment
         *     handshake the Gateway already performs, for that name, once, before it
         *     expires.
         *
         *     This is the API equivalent of the raw `INSERT INTO
         *     runtime.gateway_enrollment_token` the install guide used to require, so
         *     installing a Gateway no longer needs a database credential. Platform-RBAC
         *     gated (`gateway:enroll`) + audited.
         */
        post: operations["issueGatewayEnrollmentToken"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/gateway-enrollment-tokens/{gatewayEnrollmentTokenId}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Revoke a Gateway enrollment token (admin).
         * @description Revokes an unconsumed enrollment token so it can never be used.
         *     Idempotent. Revocation marks the token consumed rather than deleting the
         *     row: `cp_runtime` holds no DELETE on `runtime.gateway_enrollment_token`
         *     (V15 least-privilege), and the enrollment path already refuses a consumed
         *     token. Revoking an already-used token has no effect on the identity it
         *     produced — revoking an issued Gateway identity is a Lock. Platform-RBAC
         *     gated (`gateway:enroll`) + audited.
         */
        delete: operations["revokeGatewayEnrollmentToken"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/gateways": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List enrolled Gateway identities (admin).
         * @description Lists enrolled Gateways (`runtime.gateway_identity`; FR-BOOT-3) with the
         *     identity metadata an operator needs to recognise one: name, certificate
         *     fingerprint, generation counter, join method, status, the current
         *     identity's validity window, and how many node control channels it
         *     currently owns. Cursor-paginated.
         *
         *     Metadata ONLY. No certificate, no key reference, no key material — a
         *     Gateway's mTLS identity reference is not part of this projection and
         *     cannot be derived from it.
         *
         *     Platform-RBAC gated (`gateway:enroll`), not the generic `rbac:read`:
         *     `listNodes` is gated `node:enroll` on the same reasoning, and a fleet
         *     roster of Gateway names, certificate fingerprints and which Gateway
         *     fronts which nodes is targeting metadata rather than ordinary config.
         */
        get: operations["listGateways"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/gateways/{gatewayId}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                gatewayId: components["parameters"]["GatewayId"];
            };
            cookie?: never;
        };
        /**
         * Get a Gateway identity (admin).
         * @description Returns one Gateway identity by id, the same metadata-only projection the collection serves. Platform-RBAC gated (`gateway:enroll`).
         */
        get: operations["getGateway"];
        put?: never;
        post?: never;
        /**
         * Remove a Gateway identity (admin, privileged).
         * @description Removes an enrolled Gateway identity and FREES ITS NAME for
         *     re-enrollment. The common trigger is a fingerprint-pin mismatch after a
         *     restore, where the recorded identity no longer matches the Gateway that
         *     holds the key, so the name has to be released before the replacement can
         *     enroll. Idempotent. Platform-RBAC gated (`gateway:remove`) + audited.
         *
         *     The permission is deliberately NOT `gateway:enroll`. Enrollment and
         *     removal are separate verbs here for the same reason `node:enroll` and
         *     `node:remove` are separate: removal is destructive in a way enrollment is
         *     not, and a credential trusted to bring a Gateway up should not thereby be
         *     trusted to take one down.
         *
         *     **A Gateway that currently owns node control channels is refused
         *     (`409`)** unless `force=true`. Removing a live owner strands every node
         *     it fronts until another Gateway claims presence. The forced removal is
         *     recorded as a distinct audit action from the ordinary one, so the two are
         *     never conflated after the fact.
         */
        delete: operations["removeGateway"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/service-accounts": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List service accounts (admin).
         * @description Lists machine-consumer definitions (`config.service_account`; FR-AUTH-12).
         *     Issued credentials are RUNTIME and live under
         *     `/v1/service-accounts/{id}/credentials`; this resource is the DEFINITION and
         *     NEVER returns a secret. Cursor-paginated. Platform-RBAC gated (`user:manage`).
         */
        get: operations["listServiceAccounts"];
        put?: never;
        /**
         * Create a service account (admin).
         * @description Creates a machine-consumer definition. A `keyReference` that looks like
         *     private material, or a non-positive `tokenTtlSeconds`, is a `422`. The
         *     response NEVER contains a secret. Platform-RBAC gated (`user:manage`) +
         *     audited.
         */
        post: operations["createServiceAccount"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/service-accounts/{serviceAccountId}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                serviceAccountId: components["parameters"]["ServiceAccountId"];
            };
            cookie?: never;
        };
        /**
         * Get a service account (admin).
         * @description Returns one service-account definition by id (never a secret). Platform-RBAC gated (`user:manage`).
         */
        get: operations["getServiceAccount"];
        /**
         * Update a service account (admin).
         * @description Replaces a service account's mutable fields (the `name` is immutable). A bad
         *     `keyReference`/`tokenTtlSeconds` is a `422`; a stale `version` is a `409`.
         *     Platform-RBAC gated (`user:manage`) + audited.
         */
        put: operations["updateServiceAccount"];
        post?: never;
        /**
         * Delete a service account (admin).
         * @description Deletes a service-account definition (idempotent). Issued runtime credentials
         *     are revoked separately. Platform-RBAC gated (`user:manage`) + audited.
         */
        delete: operations["deleteServiceAccount"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/node-policies": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List node policies (admin).
         * @description Lists node policies (`config.node_policy`; Design §12A) — desired labels,
         *     connector kind, and declared host-trust references. Cursor-paginated.
         *     Platform-RBAC gated (`settings:write`).
         */
        get: operations["listNodePolicies"];
        put?: never;
        /**
         * Create a node policy (admin).
         * @description Creates a node policy. A bad connector kind, a non-object `desiredLabels`, or
         *     a host-trust ref that looks like private material is a `422`. Platform-RBAC
         *     gated (`settings:write`) + audited.
         */
        post: operations["createNodePolicy"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/node-policies/{nodePolicyId}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                nodePolicyId: components["parameters"]["NodePolicyId"];
            };
            cookie?: never;
        };
        /**
         * Get a node policy (admin).
         * @description Returns one node policy by id. Platform-RBAC gated (`settings:write`).
         */
        get: operations["getNodePolicy"];
        /**
         * Update a node policy (admin).
         * @description Replaces a node policy's mutable fields (the `name` is immutable). A stale
         *     `version` is a `409`. Platform-RBAC gated (`settings:write`) + audited.
         */
        put: operations["updateNodePolicy"];
        post?: never;
        /**
         * Delete a node policy (admin).
         * @description Deletes a node policy (idempotent). Platform-RBAC gated (`settings:write`) + audited.
         */
        delete: operations["deleteNodePolicy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/capability-defs": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List capability definitions (admin).
         * @description Lists the requestable-capability catalogue (`config.capability_def`; Design
         *     §12A / D14). Cursor-paginated. Platform-RBAC gated (`settings:write`).
         */
        get: operations["listCapabilityDefs"];
        put?: never;
        /**
         * Create a capability definition (admin).
         * @description Adds a capability to the catalogue. A `name` outside the closed capability
         *     set is a `422`; a duplicate is a `409`. Platform-RBAC gated (`settings:write`)
         *     + audited.
         */
        post: operations["createCapabilityDef"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/capability-defs/{capabilityDefId}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                capabilityDefId: components["parameters"]["CapabilityDefId"];
            };
            cookie?: never;
        };
        /**
         * Get a capability definition (admin).
         * @description Returns one capability definition by id. Platform-RBAC gated (`settings:write`).
         */
        get: operations["getCapabilityDef"];
        /**
         * Update a capability definition (admin).
         * @description Updates a capability definition's description (the `name` is immutable). A
         *     stale `version` is a `409`. Platform-RBAC gated (`settings:write`) + audited.
         */
        put: operations["updateCapabilityDef"];
        post?: never;
        /**
         * Delete a capability definition (admin).
         * @description Deletes a capability definition (idempotent). Platform-RBAC gated (`settings:write`) + audited.
         */
        delete: operations["deleteCapabilityDef"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/jit-policies": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List JIT policies (admin).
         * @description Lists JIT-requestable policies (`config.jit_policy`; FR-ACC-3) — requestable
         *     targets, capabilities, max TTL, and the 0-3 level approval chain.
         *     Cursor-paginated. Platform-RBAC gated (`settings:write`).
         */
        get: operations["listJitPolicies"];
        put?: never;
        /**
         * Create a JIT policy (admin).
         * @description Creates a JIT policy. `maxTtlSeconds <= 0`, an unknown capability, an
         *     approval chain longer than 3, or a malformed level is rejected pre-commit
         *     (`422`). Platform-RBAC gated (`settings:write`) + audited.
         */
        post: operations["createJitPolicy"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/jit-policies/{jitPolicyId}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                jitPolicyId: components["parameters"]["JitPolicyId"];
            };
            cookie?: never;
        };
        /**
         * Get a JIT policy (admin).
         * @description Returns one JIT policy by id. Platform-RBAC gated (`settings:write`).
         */
        get: operations["getJitPolicy"];
        /**
         * Update a JIT policy (admin).
         * @description Replaces a JIT policy's mutable fields (the `name` is immutable). Invalid
         *     config is a `422`; a stale `version` is a `409`. Platform-RBAC gated
         *     (`settings:write`) + audited.
         */
        put: operations["updateJitPolicy"];
        post?: never;
        /**
         * Delete a JIT policy (admin).
         * @description Deletes a JIT policy (idempotent). Platform-RBAC gated (`settings:write`) + audited.
         */
        delete: operations["deleteJitPolicy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/breakglass-policies": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List break-glass policies (admin).
         * @description Lists break-glass policies (`config.breakglass_policy`; FR-ACC-6) —
         *     recording-strict, alert target, review-required, and the IdP-independent
         *     auth path. Cursor-paginated. Platform-RBAC gated (`breakglass:manage`).
         */
        get: operations["listBreakglassPolicies"];
        put?: never;
        /**
         * Create a break-glass policy (admin).
         * @description Creates a break-glass policy. An empty `alertTarget` or a bad `authPath`
         *     (not fido2|offline_code) is a `422`. Platform-RBAC gated (`breakglass:manage`)
         *     + audited.
         */
        post: operations["createBreakglassPolicy"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/breakglass-policies/{breakglassPolicyId}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                breakglassPolicyId: components["parameters"]["BreakglassPolicyId"];
            };
            cookie?: never;
        };
        /**
         * Get a break-glass policy (admin).
         * @description Returns one break-glass policy by id. Platform-RBAC gated (`breakglass:manage`).
         */
        get: operations["getBreakglassPolicy"];
        /**
         * Update a break-glass policy (admin).
         * @description Replaces a break-glass policy's mutable fields (the `name` is immutable). A
         *     bad `authPath`/empty `alertTarget` is a `422`; a stale `version` is a `409`.
         *     Platform-RBAC gated (`breakglass:manage`) + audited.
         */
        put: operations["updateBreakglassPolicy"];
        post?: never;
        /**
         * Delete a break-glass policy (admin).
         * @description Deletes a break-glass policy (idempotent). Platform-RBAC gated (`breakglass:manage`) + audited.
         */
        delete: operations["deleteBreakglassPolicy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/session-limit-policies": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List session-limit policies (admin).
         * @description Lists per-identity session-limit policies (`config.session_limit_policy`;
         *     FR-SESS-3) — overrides for the concurrent-session cap, max session
         *     duration, and idle timeout. When several policies match an identity the
         *     most restrictive value wins per knob; the `operator_settings` cluster
         *     defaults apply where no policy matches. Cursor-paginated. Platform-RBAC
         *     gated (`rbac:read`).
         */
        get: operations["listSessionLimitPolicies"];
        put?: never;
        /**
         * Create a session-limit policy (admin).
         * @description Creates a session-limit policy. Invalid config (a malformed
         *     `identitySelector`, a non-positive limit, or all three limits absent) is
         *     rejected PRE-COMMIT (`422`, FR-API-5). The `origin` is server-set to
         *     `api`. Platform-RBAC gated (`settings:write`) + audited.
         */
        post: operations["createSessionLimitPolicy"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/session-limit-policies/{sessionLimitPolicyId}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                sessionLimitPolicyId: components["parameters"]["SessionLimitPolicyId"];
            };
            cookie?: never;
        };
        /**
         * Get a session-limit policy (admin).
         * @description Returns one session-limit policy by id. Platform-RBAC gated (`rbac:read`).
         */
        get: operations["getSessionLimitPolicy"];
        /**
         * Update a session-limit policy (admin).
         * @description Replaces the mutable fields of a session-limit policy (the `name` is
         *     immutable). Invalid config is rejected pre-commit (`422`). The `version`
         *     is required and must match (optimistic concurrency — a stale version is
         *     a `409`). Platform-RBAC gated (`settings:write`) + audited.
         */
        put: operations["updateSessionLimitPolicy"];
        post?: never;
        /**
         * Delete a session-limit policy (admin).
         * @description Deletes a session-limit policy (idempotent — `204` whether or not it
         *     existed). Enforcement falls back to the remaining policies / cluster
         *     defaults from the next decision. Platform-RBAC gated (`settings:write`)
         *     + audited.
         */
        delete: operations["deleteSessionLimitPolicy"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/operator-settings": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get the cluster operator settings (admin).
         * @description Returns the cluster-wide operator settings (`config.operator_settings`) —
         *     audit/recording retention, the WORM default for new recordings, the OTP
         *     TTL, the session-limit cluster defaults (FR-SESS-3), and whether the
         *     customer recording key is provisioned. The row is a singleton written by
         *     cold start, so it is addressed by this path and has no id.
         *
         *     The projection is explicit and narrower than the row. The
         *     key-encryption-key reference is NEVER returned, for two independent
         *     reasons. It is dead: no code path reads or writes that column, and the
         *     key-encryption reference that is actually live is a deployment property
         *     persisted per key alongside the key material it wraps — so exposing this
         *     one would ship a knob an operator could set and reasonably believe had
         *     repointed the KEK. And were it ever wired up, its whole content would be
         *     *where the platform's most sensitive key lives*, which is a targeting
         *     hint for the very asset it protects rather than a setting anyone reads
         *     back. The `bootstrap*` fields are owned by the first-admin bootstrap flow
         *     and are not settings.
         *     Recording-key BYTES are not here either; this resource reports only
         *     `recordingKeyConfigured` and the sub-resource serves the key itself.
         *
         *     Platform-RBAC gated (`rbac:read`).
         */
        get: operations["getOperatorSettings"];
        /**
         * Update the cluster operator settings (admin).
         * @description Replaces the writable operator settings. The `version` is required and
         *     must match (optimistic concurrency — a stale version is a `409`). The
         *     `origin` is server-set to `api`. Platform-RBAC gated (`settings:write`),
         *     audited before/after.
         *
         *     **The evidence-retention knobs move one way only.** `auditRetentionDays`
         *     and `recordingRetentionDays` accept an increase or an unchanged value,
         *     and `defaultWormMode` accepts `governance` -> `compliance`; the opposite
         *     direction is a `422` — at this scope and at every other. Shortening audit
         *     retention makes the next prune drop partitions it would otherwise have
         *     kept, shortening recording retention shortens the object-lock
         *     retain-until stamped on new recordings, and `compliance` ->
         *     `governance` makes new recordings deletable instead of un-deletable.
         *     Those are one-call anti-forensics primitives, and they do not become
         *     reachable merely because this resource now has an API. Weakening one
         *     stays a deliberate, out-of-band act by the database owner.
         *
         *     **Deployment-pinned fields are refused, never silently reverted.** Each
         *     of `defaultMaxSessionSeconds`, `defaultIdleTimeoutSeconds` and
         *     `defaultMaxConcurrentSessions` is reconciled from a deployment property
         *     on every boot when that property is set; `deploymentManagedFields` in the
         *     `GET` reports which of them are pinned right now. Changing a pinned field
         *     here is a `422` naming the property that owns it, because accepting the
         *     write would ship a setting that reverts at the next restart. Submitting a
         *     pinned field with its value unchanged is not a change and is accepted, so
         *     a read-modify-write of the whole resource still works.
         *
         *     `defaultCaBackend` is read-only and is not part of the request schema (a
         *     `400`): it is consumed only when cold start provisions a CA kind that has
         *     no row yet, so once the CAs exist, changing it changes nothing. The
         *     backend that governs a live CA is `/v1/cas`.
         */
        put: operations["updateOperatorSettings"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/operator-settings/recording-customer-key": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get the customer recording key (admin).
         * @description Returns the provisioned customer recording key — PUBLIC material only
         *     (FR-AUD-2, Design §15). The Gateway seals every per-recording data key to
         *     this public key and the private half never reaches the platform, so the
         *     platform cannot decrypt its own recordings.
         *
         *     An unprovisioned key is a `200` with `configured: false`, not a `404`:
         *     "not provisioned yet" is the normal state of a fresh install, not a
         *     missing resource. Platform-RBAC gated (`rbac:read`).
         */
        get: operations["getRecordingCustomerKey"];
        /**
         * Provision or rotate the customer recording key (admin, privileged).
         * @description Provisions the customer recording key, or rotates it. With strict
         *     recording on — the shipped default — a Control Plane with no key refuses
         *     every session, so this is the operation that lets a first install be
         *     completed holding an API credential rather than a database one.
         *
         *     Platform-RBAC gated on **`recording:key-manage`**, a permission of its
         *     own. Its holder can point future recordings at a public key whose private
         *     half they control, which is a privilege of a different kind from anything
         *     else this resource confers: `settings:write` is already full config
         *     administration, but it has never been able to break the property that the
         *     platform cannot read its own recordings, and folding the key into it
         *     would hand that to every config administrator. The permission is implied
         *     by no other and implies none.
         *
         *     Only PUBLIC material is ever stored, and the endpoint proves that before
         *     it writes anything: the submission must base64-decode to a non-empty DER
         *     SubjectPublicKeyInfo of at most 8 KiB, must not be private key material
         *     in any encoding (PKCS#8 `PrivateKeyInfo`, SEC1 `ECPrivateKey`, or PEM
         *     text of any kind), and must match `sealAlgorithm` — EC on P-256 for
         *     `ecies_p256`, RSA for `rsa_oaep_sha256`. Each failure is a `422` that
         *     says which, so a curve mismatch surfaces here rather than as a
         *     fail-closed session at the worst possible moment. `keyRef` is a reference
         *     only and is refused the same way if it carries key material.
         *
         *     **Rotation is allowed and it is not quiet.** Every recording sealed under
         *     the outgoing key remains readable only by the outgoing private key; the
         *     incoming key cannot read them. So a rotation — any write when a key is
         *     already configured — additionally requires `expectedFingerprintSha256`
         *     echoing the key being replaced (a blind or racing overwrite is a `409`)
         *     and `acknowledgeExistingRecordingsUndecryptable: true` (a `422`
         *     otherwise). First provisioning, where no key and no recording exists,
         *     requires neither.
         *
         *     There is no `DELETE`: un-provisioning the key would fail-close every
         *     future session, and that is not an operator task.
         *
         *     Audited before/after, carrying FINGERPRINTS only — never key bytes.
         */
        put: operations["setRecordingCustomerKey"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/sessions": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List SSH sessions (admin).
         * @description Lists SSH sessions (`runtime.ssh_session`; Design §12A) with their decision
         *     snapshot (identity, node, principal, access model, capabilities, grant
         *     expiry). Optional filters narrow by identity, node, access model, and
         *     active-only. Cursor-paginated. Platform-RBAC gated (`audit:read`).
         */
        get: operations["listSessions"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/sessions/{sessionId}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                sessionId: components["parameters"]["SessionId"];
            };
            cookie?: never;
        };
        /**
         * Get an SSH session (admin).
         * @description Returns one session by id. Platform-RBAC gated (`audit:read`).
         */
        get: operations["getSession"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/sessions/{sessionId}/terminate": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                sessionId: components["parameters"]["SessionId"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Terminate an SSH session (admin).
         * @description Tears down a live session by pushing a top-tier `Lock` (Design §8.3/§8.4)
         *     scoped to the session's subject identity, reusing the teardown path — a
         *     deny that wins and is actively pushed to every Gateway (fail-closed). Because
         *     the wire `Lock` selector has no per-session facet, the teardown is
         *     identity-scoped (it also affects that identity's other live sessions); it is
         *     bounded by a short TTL so the identity may immediately reconnect under an
         *     unchanged policy. Idempotent. Platform-RBAC gated (`lock:write`) + audited.
         */
        post: operations["terminateSession"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/session-leases": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List concurrency leases (admin).
         * @description Lists FR-SESS-3 concurrency leases (`runtime.session_lease`) — the rows
         *     the per-identity cap is counted from. This is the diagnostic half, and
         *     the sharper of the two gaps it closes: an identity blocked by a lease
         *     that outlived its session is refused with the same generic policy denial
         *     as a real deny, so without this an operator cannot tell the two apart
         *     until the lease's safety TTL is reaped.
         *
         *     A lease counts while `countsTowardCap` is true, which is unreleased AND
         *     unexpired. So a ghost is not "expired but still counted" — an expired
         *     lease has already stopped counting, and the reaper only tidies the row.
         *     It is a lease that STILL counts with no live session behind it: compare
         *     this collection filtered by `identity` with `activeOnly=true` against
         *     `GET /v1/sessions` filtered the same way, and the difference is the
         *     over-count. A lease carrying no `expiresAt` counts until something
         *     releases it, which is the unbounded case. Cursor-paginated.
         *     Platform-RBAC gated (`audit:read`), the same permission that reads
         *     `/v1/sessions`.
         */
        get: operations["listSessionLeases"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/session-leases/{sessionLeaseId}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                sessionLeaseId: components["parameters"]["SessionLeaseId"];
            };
            cookie?: never;
        };
        /**
         * Get a concurrency lease (admin).
         * @description Returns one lease by id — the confirmation step before releasing it. Platform-RBAC gated (`audit:read`).
         */
        get: operations["getSessionLease"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/session-leases/{sessionLeaseId}/release": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                sessionLeaseId: components["parameters"]["SessionLeaseId"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Release one concurrency lease (admin, privileged).
         * @description Releases ONE stuck lease by id, freeing the slot it occupies. The `reason`
         *     is required and audited — this is a correction to enforcement state, and
         *     an unexplained one is indistinguishable after the fact from an operator
         *     quietly raising somebody's cap. Idempotent: releasing an
         *     already-released lease is a `200` with the lease unchanged.
         *
         *     **There is deliberately no bulk form and no release-by-identity
         *     parameter.** Releasing every lease for an identity trades a bounded
         *     over-count for an under-count, which lets that identity exceed the cap
         *     for as long as the real sessions run — a worse failure than the one being
         *     fixed, and the dangerous shape is therefore not offered at all. Diagnose
         *     with the collection, then release the specific lease.
         *
         *     Platform-RBAC gated (`lock:write`), the same permission that terminates a
         *     session, because both correct live enforcement state.
         */
        post: operations["releaseSessionLease"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/recordings": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List session recordings (admin).
         * @description Lists session recording metadata (`runtime.recording_ref`; Design §12).
         *     NEVER returns recording bytes — replay/export issue short-lived signed URLs.
         *     Optional filters narrow by session, node, and identity. Cursor-paginated.
         *     Platform-RBAC gated (`recording:replay`).
         */
        get: operations["listRecordings"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/recordings/{recordingId}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                recordingId: components["parameters"]["RecordingId"];
            };
            cookie?: never;
        };
        /**
         * Get session recording metadata (admin).
         * @description Returns one recording's metadata (never bytes). Platform-RBAC gated
         *     (`recording:replay`).
         */
        get: operations["getRecording"];
        put?: never;
        post?: never;
        /**
         * Governance-delete a recording (admin, privileged).
         * @description Governance-mode erasure escape hatch (Design §12.2/§12.3, FR-AUD-3/6): a
         *     specifically-privileged, audited role deletes the encrypted recording object
         *     and marks the metadata pruned. Platform-RBAC gated (`recording:delete`) +
         *     audited (FR-PADM-3). Refused (`409`) for a **compliance**-mode recording
         *     (truly un-deletable, object-lock) or one under **legal hold**. Idempotent —
         *     `204` whether or not the object was still present.
         */
        delete: operations["deleteRecording"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/recordings/{recordingId}/replay": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                recordingId: components["parameters"]["RecordingId"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Issue a replay signed URL (admin).
         * @description Issues a short-lived, single-object SIGNED URL for replaying a recording
         *     (Design §12.2/§18) — bytes NEVER proxy through the CP and the object stays
         *     customer-key encrypted (the CP cannot decrypt); the access is itself audited
         *     (FR-AUD-5/FR-PADM-3). Platform-RBAC gated (`recording:replay`), scopable by
         *     node-label/user/time (FR-PADM-2).
         */
        post: operations["replayRecording"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/recordings/{recordingId}/export": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                recordingId: components["parameters"]["RecordingId"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Issue an export signed URL (admin).
         * @description Issues a short-lived, single-object SIGNED URL for exporting a recording
         *     (Design §12.2/§18) — bytes NEVER proxy through the CP and the object stays
         *     customer-key encrypted (the CP cannot decrypt); itself audited (FR-AUD-5).
         *     Platform-RBAC gated (`recording:export`), scopable (FR-PADM-2).
         */
        post: operations["exportRecording"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/recordings/{recordingId}/legal-hold": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                recordingId: components["parameters"]["RecordingId"];
            };
            cookie?: never;
        };
        get?: never;
        /**
         * Set or release a recording's legal hold (admin, privileged).
         * @description Places or releases a legal hold on a recording (Design §12.3, FR-AUD-6). A
         *     held recording is exempt from retention pruning AND governance delete in
         *     either WORM mode. Platform-RBAC gated (`recording:delete`) + audited
         *     (FR-PADM-3). Idempotent by desired state.
         */
        put: operations["setRecordingLegalHold"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/audit-events": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Search the correlated audit stream (admin).
         * @description Searches the correlated audit stream (`runtime.audit_event`; Design §12,
         *     FR-AUD-8/9) — one append-only stream for SSH-session and web/admin events,
         *     correlated by id, newest-first. Search dimensions (FR-AUD-8): identity
         *     (`actor`), subject, action, outcome, node (`nodeId`) and node label
         *     (`nodeLabel`), session, source IP, capability, access model, and a time
         *     range; `correlationId` reconstructs one full path (approve → connect → run →
         *     replay, FR-AUD-9). Cursor-paginated. Read-only — a search never mutates the
         *     append-only stream and the hash chain stays verifiable. Platform-RBAC gated
         *     (`audit:read`); results are additionally filtered to the caller's RBAC scope
         *     (a node-label/user/time-scoped `audit:read` grant returns only in-scope
         *     events; an unscoped grant sees all).
         */
        get: operations["searchAuditEvents"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/audit-events/{auditEventId}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                auditEventId: components["parameters"]["AuditEventId"];
            };
            cookie?: never;
        };
        /**
         * Get one audit event (admin).
         * @description Returns one audit event by id. Platform-RBAC gated (`audit:read`).
         */
        get: operations["getAuditEvent"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /**
         * RFC 9457 Problem Details
         * @description Problem details for HTTP APIs as defined by RFC 9457 (obsoletes RFC 7807).
         *     Error responses use media type `application/problem+json`.
         */
        ProblemDetails: {
            /**
             * Format: uri-reference
             * @description A URI reference identifying the problem type.
             * @default about:blank
             */
            type: string;
            /** @description A short, human-readable summary of the problem type. */
            title?: string;
            /**
             * Format: int32
             * @description The HTTP status code.
             */
            status?: number;
            /** @description A human-readable explanation specific to this occurrence. */
            detail?: string;
            /**
             * Format: uri-reference
             * @description A URI reference identifying the specific occurrence.
             */
            instance?: string;
        } & {
            [key: string]: unknown;
        };
        /**
         * Health Status
         * @description Liveness/readiness result, loosely modelled on the IETF health-check draft.
         */
        HealthStatus: {
            /**
             * @description Overall health indicator.
             * @enum {string}
             */
            status: "pass" | "fail" | "warn";
        };
        /**
         * Protocol Version Range
         * @description An inclusive range of supported protocol versions, `major.minor` strings.
         */
        ProtocolVersionRange: {
            /** @description Inclusive lowest supported protocol version, e.g. "1.0". */
            min: string;
            /** @description Inclusive highest supported protocol version, e.g. "1.0". */
            max: string;
        };
        /**
         * Version Info
         * @description Component build version and the protocol ranges it negotiates.
         */
        VersionInfo: {
            /** @description Formal component name. */
            component: string;
            /** @description Component build version (SemVer 2.0.0). */
            version: string;
            /**
             * Protocol Ranges
             * @description Supported protocol version ranges by plane.
             */
            protocols: {
                controlPlaneGatewayGrpc: components["schemas"]["ProtocolVersionRange"];
                agentGatewayWire: components["schemas"]["ProtocolVersionRange"];
            };
        };
        /** Bootstrap Claim Request */
        BootstrapClaimRequest: {
            /** @description The printed-once bootstrap credential (Design §2A). Its SHA-256 is compared to the armed hash in constant time; this value is the sole authenticator for the call. */
            credential: string;
            /** @description The identity (e.g. OIDC subject) to bind as the first platform admin. */
            subject: string;
        };
        /** Bootstrap Claim Result */
        BootstrapClaimResult: {
            /**
             * @description `not_available` means no credential is currently armed (config-named admin or already provisioned by that path). Note: unlike every other error response in this contract, this body is a plain JSON object, not an RFC 9457 problem document (see `contracts/openapi/README.md`).
             * @enum {string}
             */
            status: "provisioned" | "already_completed" | "not_available" | "invalid_credential";
        };
        /**
         * OAuth Token Request
         * @description Client-credentials token request (Design §5.6). Sent as
         *     `application/x-www-form-urlencoded`. Client authentication is by
         *     `client_assertion` (private_key_jwt) or by the mTLS client certificate;
         *     a static `client_secret` is accepted but discouraged.
         */
        TokenRequest: {
            /** @description Must be `client_credentials`. */
            grant_type: string;
            client_id?: string;
            /** @description For private_key_jwt: urn:ietf:params:oauth:client-assertion-type:jwt-bearer. */
            client_assertion_type?: string;
            /** @description The signed JWT client assertion (RFC 7523). */
            client_assertion?: string;
            /** @description Discouraged static secret (only if the SA uses client_secret). */
            client_secret?: string;
            scope?: string;
        };
        /** OAuth Token Response */
        TokenResponse: {
            /** @description The short-lived CP-signed bearer token. */
            access_token: string;
            /** @description Always `Bearer`. */
            token_type: string;
            /**
             * Format: int64
             * @description Token lifetime in seconds.
             */
            expires_in: number;
            scope?: string;
        };
        /** Issue OTP Request */
        IssueOtpRequest: {
            /** @description The identity the OTP authenticates (taken from the record, never client input at validation). */
            identity: string;
            allowedPrincipals: string[];
            /** @description Optional source-CIDR binding (deny-only reducer). */
            sourceCidr?: string;
            /**
             * Format: int32
             * @description OTP lifetime (60–300s); defaults to the operator setting.
             */
            ttlSeconds?: number;
        };
        /** Issued OTP */
        IssuedOtp: {
            /** Format: uuid */
            otpId: string;
            /** @description The raw OTP, returned ONCE for out-of-band delivery; only its hash is stored. */
            otp: string;
            /** Format: date-time */
            expiresAt: string;
        };
        /** Create Pin Request */
        CreatePinRequest: {
            /** @description The pinned public-key fingerprint (SHA256:...). */
            fingerprint: string;
            identity: string;
            /** @description Source-CIDR binding (deny-only reducer, FR-AUTH-15). */
            sourceCidr?: string;
            principals: string[];
            /**
             * Format: int64
             * @description Pin lifetime; capped at the authorization TTL (FR-AUTH-10).
             */
            ttlSeconds: number;
        };
        /** Pin */
        PinResource: {
            /** Format: uuid */
            id: string;
            fingerprint: string;
            identity: string;
            sourceCidr?: string;
            principals: string[];
            /** Format: date-time */
            expiresAt: string;
            /** Format: date-time */
            revokedAt?: string;
        };
        /** Pin List */
        PinList: {
            pins: components["schemas"]["PinResource"][];
        };
        /** Issue Service-Account Credential Request */
        IssueServiceAccountCredentialRequest: {
            /** @enum {string} */
            credentialType: "private_key_jwt" | "mtls" | "client_secret";
            /** @description For private_key_jwt — the client's public key (PEM). Public material only. */
            publicKeyPem?: string;
            /** @description For private_key_jwt — a JWKS URI reference (alternative to publicKeyPem). */
            jwksUri?: string;
            /** @description For mtls — the client certificate SHA-256 fingerprint. */
            certificateFingerprint?: string;
            /**
             * Format: int64
             * @description Credential validity; null = non-expiring until revoked.
             */
            ttlSeconds?: number;
        };
        /** Service-Account Credential */
        ServiceAccountCredentialResource: {
            /** Format: uuid */
            id: string;
            /** Format: uuid */
            serviceAccountId: string;
            credentialType: string;
            fingerprint?: string;
            /** @description For client_secret — the raw secret, returned ONCE; only its hash is stored. */
            clientSecret?: string;
            status: string;
            /** Format: date-time */
            issuedAt: string;
            /** Format: date-time */
            notAfter?: string;
        };
        /** Begin Device Flow Request */
        BeginDeviceFlowRequest: {
            /** @description The SSH source IP to bind for anti-phishing correlation (§5.2). */
            sourceIp?: string;
            /** @description An opaque 1:1 device-code↔connection binding value. */
            connectionBinding?: string;
        };
        /** Device Flow */
        DeviceFlowResource: {
            /** Format: uuid */
            deviceFlowId: string;
            /** @description The short code the user enters at the verification page. */
            userCode: string;
            /** @description The CP-hosted verification page URL. */
            verificationUri: string;
            /** @description Verification URI with the user code embedded (convenience). */
            verificationUriComplete?: string;
            /** @description The opaque device code the caller polls with (its own credential). */
            deviceCode: string;
            /**
             * Format: int32
             * @description Minimum poll interval in seconds.
             */
            interval: number;
            /**
             * Format: int64
             * @description Seconds until the flow expires.
             */
            expiresIn: number;
        };
        /** Poll Device Flow Request */
        PollDeviceFlowRequest: {
            deviceCode: string;
        };
        /** Device Flow Status */
        DeviceFlowStatus: {
            /** @enum {string} */
            status: "pending" | "authorized" | "denied" | "expired";
            /** @description The resolved identity, present when status=authorized. */
            identity?: string;
            /** @description Whether the approving browser source correlated with the SSH source (§5.2). */
            sourceContextMatch?: boolean;
        };
        /**
         * Lock Mode
         * @description Lock enforcement mode (Design §8.3). `strict` tears down matching live
         *     sessions and blocks new ones; `best_effort` blocks new issuance but does
         *     not forcibly tear down an already-established session.
         * @enum {string}
         */
        LockMode: "strict" | "best_effort";
        /**
         * Lock Target
         * @description What the lock matches (LockMatching facets; OR-matched). At least one
         *     facet must be non-empty, OR `all` must be true — a global lock is never
         *     implicit (ingest rejects an empty target).
         */
        LockTarget: {
            /** @description Resolved subject identities to deny. */
            identities?: string[];
            /** @description SSO/OIDC groups to deny (membership match). */
            groups?: string[];
            /** @description Target node ids to deny. */
            nodeIds?: string[];
            /** @description Linux logins (principals) to deny. */
            principals?: string[];
            /** @description Node labels ("key=value") to deny. */
            nodeLabels?: string[];
            /** @description An explicit fleet-wide lock (deny everything). Required for a target with no other facet. */
            all?: boolean;
        };
        /** Create Lock Request */
        CreateLockRequest: {
            target: components["schemas"]["LockTarget"];
            /** @description Operator/audit reason (mandatory). Never disclosed to the SSH user (§7.1). */
            reason: string;
            /** @description Enforcement mode; omitted defaults to `strict` (backward-compatible). */
            mode?: components["schemas"]["LockMode"];
            /**
             * Format: int64
             * @description Optional lock lifetime in seconds; omitted or null = no expiry (indefinite until released).
             */
            ttlSeconds?: number;
        };
        /** Lock */
        LockResource: {
            /** Format: uuid */
            id: string;
            target: components["schemas"]["LockTarget"];
            reason: string;
            /** @description Enforcement mode (`strict` or `best_effort`). */
            mode?: components["schemas"]["LockMode"];
            /**
             * Format: date-time
             * @description Absolute expiry; absent when the lock has no TTL.
             */
            expiresAt?: string;
            /** Format: date-time */
            createdAt: string;
            /** @description The admin identity that created the lock. */
            createdBy?: string;
        };
        /** Lock List */
        LockList: {
            locks: components["schemas"]["LockResource"][];
        };
        /**
         * Issue Join Token Request
         * @description Request to mint a single-use agent join token bound to a node.
         */
        IssueJoinTokenRequest: {
            /** @description The stable node identity the token authorizes an agent to join as (FR-JOIN-6). The token is scoped to exactly this node. */
            nodeName: string;
            /**
             * Format: int32
             * @description Optional TTL override (seconds). Omitted uses the configured default; the value is clamped to the configured maximum. Join tokens are short-lived by design.
             */
            ttlSeconds?: number;
        };
        /**
         * Issued Join Token
         * @description A freshly issued join token. The `token` field is the raw single-use bearer value and is returned exactly ONCE; only its hash is stored.
         */
        IssuedJoinToken: {
            /** Format: uuid */
            id: string;
            /** @description The raw single-use join token. Shown once; never retrievable again. */
            token: string;
            nodeName: string;
            /** @enum {string} */
            joinMethod: "token";
            singleUse: boolean;
            /** Format: date-time */
            expiresAt: string;
        };
        /**
         * Join Token
         * @description Join-token metadata (never the raw token).
         */
        JoinTokenResource: {
            /** Format: uuid */
            id: string;
            nodeName: string;
            /** @enum {string} */
            joinMethod: "token";
            singleUse: boolean;
            /** Format: date-time */
            expiresAt: string;
            /** Format: date-time */
            createdAt: string;
            /** @description The admin identity that issued the token. */
            createdBy?: string;
        };
        /** Join Token List */
        JoinTokenList: {
            joinTokens: components["schemas"]["JoinTokenResource"][];
        };
        /**
         * Issue Gateway Enrollment Token Request
         * @description Request to mint a single-use enrollment token bound to a Gateway.
         */
        IssueGatewayEnrollmentTokenRequest: {
            /** @description The stable Gateway name the token authorises an enrollment as. The token is scoped to exactly this name; enrolling under any other name is refused (FR-JOIN-3). */
            gatewayName: string;
            /**
             * Format: int32
             * @description Optional TTL override (seconds). Omitted uses the configured default; the value is clamped to the configured maximum. Enrollment tokens are short-lived by design.
             */
            ttlSeconds?: number;
        };
        /**
         * Issued Gateway Enrollment Token
         * @description A freshly issued Gateway enrollment token. The `token` field is the raw single-use bearer value and is returned exactly ONCE; only its hash is stored, so it can never be read back.
         */
        IssuedGatewayEnrollmentToken: {
            /** Format: uuid */
            id: string;
            /** @description The raw single-use enrollment token. Shown once; never retrievable again. */
            token: string;
            gatewayName: string;
            singleUse: boolean;
            /** Format: date-time */
            expiresAt: string;
        };
        /**
         * Gateway Enrollment Token
         * @description Enrollment-token metadata (never the raw token).
         */
        GatewayEnrollmentTokenResource: {
            /** Format: uuid */
            id: string;
            gatewayName: string;
            singleUse: boolean;
            /** Format: date-time */
            expiresAt: string;
            /** Format: date-time */
            createdAt: string;
            /** @description The admin identity that issued the token. */
            createdBy?: string;
        };
        /** Gateway Enrollment Token List */
        GatewayEnrollmentTokenList: {
            gatewayEnrollmentTokens: components["schemas"]["GatewayEnrollmentTokenResource"][];
        };
        /**
         * Internal mTLS CA Trust Anchor
         * @description The active internal mTLS CA certificate — public material only. No private key, key reference, or wrapped material appears here or can be derived from it.
         */
        MtlsTrustAnchor: {
            /** @description The X.509 CA certificate, PEM-encoded. */
            pem: string;
            /** @description Lowercase hex SHA-256 over the certificate DER, for out-of-band verification that the anchor an operator installed is the one the Control Plane serves. */
            fingerprintSha256: string;
            /** @description The certificate's subject distinguished name. */
            subject: string;
            /** Format: date-time */
            notBefore: string;
            /**
             * Format: date-time
             * @description When the anchor expires. Every enrolled Gateway must be re-anchored before this instant.
             */
            notAfter: string;
        };
        /**
         * Gateway Identity
         * @description An enrolled Gateway's identity METADATA. NEVER carries a certificate, a key reference, or key material — the mTLS identity reference is outside this projection and cannot be derived from it.
         */
        GatewayResource: {
            /** Format: uuid */
            id: string;
            /** @description The stable Gateway name. Removal frees it for re-enrollment, and node presence records the OWNER BY NAME, so a name reused after a removal inherits whatever presence still refers to it. */
            name: string;
            /** @description Lowercase hex SHA-256 over the current mTLS identity certificate's DER. A mismatch against what the Gateway actually holds — the usual after-restore symptom — is visible here. */
            fingerprintSha256?: string;
            /** @description The previous identity certificate's digest, retained across renewal. The Control Plane admits a caller matching EITHER this or the current fingerprint, so a Gateway presenting this one is mid-renewal and healthy. Without it a mismatch is ambiguous, and the two readings call for opposite actions: leave it alone, or free the name and re-enroll. Removing a Gateway that was fine is the expensive mistake here, which is why the field is on the resource that informs that decision. */
            prevFingerprintSha256?: string;
            /**
             * Format: int64
             * @description Renewal generation counter. A credential presenting an older generation is a stale clone and is refused.
             */
            generation: number;
            /** @enum {string} */
            joinMethod: "token" | "oidc" | "mtls";
            /** @enum {string} */
            status: "active" | "locked" | "revoked";
            /**
             * Format: date-time
             * @description When the CURRENT identity certificate was issued; a renewal moves it.
             */
            issuedAt?: string;
            /**
             * Format: date-time
             * @description When the current identity certificate expires.
             */
            notAfter?: string;
            /**
             * Format: int32
             * @description How many node control channels this Gateway currently owns. Non-zero means removing it strands those nodes until another Gateway claims presence, which is why removal refuses without `force`.
             */
            presenceNodeCount: number;
            /**
             * Format: date-time
             * @description The most recent presence heartbeat from this Gateway; absent if it owns nothing.
             */
            presenceLastSeenAt?: string;
            /**
             * Format: date-time
             * @description When the Gateway first enrolled.
             */
            createdAt?: string;
            /** Format: date-time */
            updatedAt?: string;
        };
        /** Gateway Page */
        GatewayPage: {
            items: components["schemas"]["GatewayResource"][];
            /** @description Cursor for the next page; absent on the last page. */
            nextCursor?: string;
        };
        /**
         * Register Node Request
         * @description Enrol an agentless node. `address` (host:port) is required; at least one of
         *     `hostCertificate` / `pinnedHostKey` is required (host-identity anchor — no
         *     TOFU, §9.3).
         */
        RegisterNodeRequest: {
            /** @description The node's stable, unique human name (the enrollment key + the SSH addressing name). */
            name: string;
            /** @description The node sshd dial address, `host:port` (port defaults to 22 if omitted). */
            address: string;
            /** @description Inventory labels ("key=value") used by data-plane rule/lock node selectors. */
            labels?: {
                [key: string]: string;
            };
            /** @description The node's host-CA-signed OpenSSH host certificate line (the primary host-identity anchor). */
            hostCertificate?: string;
            /** @description An explicitly pinned OpenSSH host public-key line (the fallback anchor when the node presents a plain host key). */
            pinnedHostKey?: string;
            /** @description Optional NodePolicy snapshot reference. */
            nodePolicyName?: string;
        };
        /** Quarantine Node Request */
        QuarantineNodeRequest: {
            /** @description Operator/audit reason (mandatory). Never disclosed to the SSH user (§7.1). */
            reason: string;
            /**
             * @description How existing sessions are handled — `kill` (torn down at once) or `drain` (finish; no new channels).
             * @default kill
             * @enum {string}
             */
            existingSessions: "kill" | "drain";
            /**
             * Format: int64
             * @description Optional quarantine-lock lifetime in seconds; omitted/null = indefinite until released.
             */
            ttlSeconds?: number;
        };
        /** Node */
        NodeResource: {
            /** Format: uuid */
            id: string;
            name: string;
            /** @enum {string} */
            connectorKind: "agent" | "agentless";
            /** @enum {string} */
            status: "pending" | "active" | "quarantined" | "removed";
            /** @enum {string} */
            health: "unknown" | "healthy" | "unhealthy" | "unreachable";
            address?: string;
            labels?: {
                [key: string]: string;
            };
            /** @description The Gateway currently owning the node's agent control channel (agent model); absent otherwise. */
            owningGateway?: string;
            statusReason?: string;
            statusChangedBy?: string;
            /** Format: date-time */
            statusChangedAt?: string;
            /** Format: date-time */
            createdAt?: string;
            /** Format: date-time */
            updatedAt?: string;
        };
        /** Node List */
        NodeList: {
            nodes: components["schemas"]["NodeResource"][];
        };
        /**
         * JIT Request Submission
         * @description A just-in-time access request. The requester is the authenticated caller, never a field.
         */
        JitRequestSubmission: {
            /**
             * Format: uuid
             * @description The target node to request JIT access to.
             */
            targetNodeId: string;
            /** @description The Linux login requested (must be within what the JIT policy allows). */
            principal: string;
            /** @description Requested capabilities (narrowed to the policy's set; default = the policy set). */
            capabilities?: string[];
            /** @description The justification for the request (audited). */
            reason: string;
        };
        /**
         * JIT Decision Request
         * @description The optional reason attached to an approve/deny/revoke action.
         */
        JitDecisionRequest: {
            reason?: string;
        };
        /** JIT Approval Level */
        JitApprovalLevel: {
            /** @enum {string} */
            kind?: "email" | "oidc_group";
            value?: string;
        };
        /** JIT Approval Entry */
        JitApproval: {
            approver?: string;
            /** Format: int32 */
            level?: number;
            decision?: string;
            reason?: string;
            at?: string;
        };
        /** JIT Request */
        JitRequestResource: {
            /** Format: uuid */
            id: string;
            requester: string;
            /** Format: uuid */
            targetNodeId?: string;
            targetNodeName?: string;
            principal: string;
            capabilities?: string[];
            reason: string;
            /** @description REQUESTED | PENDING_APPROVAL | APPROVED | DENIED | EXPIRED | ACTIVE | REVOKED. */
            state: string;
            jitPolicyName?: string;
            approvalChain?: components["schemas"]["JitApprovalLevel"][];
            approvals?: components["schemas"]["JitApproval"][];
            /** Format: date-time */
            approvalDeadline?: string;
            /** Format: date-time */
            grantExpiresAt?: string;
            /** Format: date-time */
            requestedAt: string;
            /** Format: date-time */
            decidedAt?: string;
            decidedBy?: string;
        };
        /** JIT Request List */
        JitRequestList: {
            jitRequests: components["schemas"]["JitRequestResource"][];
        };
        /**
         * Register Break-glass Credential Request
         * @description Registers a break-glass FIDO2 sk-ecdsa PUBLIC key. Public material only.
         */
        RegisterBreakglassCredentialRequest: {
            /**
             * Format: byte
             * @description The OpenSSH sk-ecdsa-sha2-nistp256 wire pubkey blob (base64). PUBLIC.
             */
            publicKey: string;
            /** @description The break-glass operator identity this key resolves to. */
            identity: string;
            allowedPrincipals: string[];
            /** @description Optional node scope (a fleet credential omits it). */
            nodeIds?: string[];
            /**
             * Format: date-time
             * @description Optional expiry (a hardware token is durable; omit for none).
             */
            expiresAt?: string;
        };
        /** Break-glass Credential */
        BreakglassCredentialResource: {
            /** Format: uuid */
            id: string;
            /** @description The OpenSSH SHA256 fingerprint of the registered key. */
            keyFingerprint: string;
            skApplication?: string;
            identity: string;
            allowedPrincipals: string[];
            nodeIds?: string[];
            /** Format: date-time */
            expiresAt?: string;
            /** Format: date-time */
            revokedAt?: string;
            createdBy?: string;
            /** Format: date-time */
            createdAt: string;
        };
        /** Break-glass Credential List */
        BreakglassCredentialList: {
            credentials: components["schemas"]["BreakglassCredentialResource"][];
        };
        /** Issue Break-glass Offline Codes Request */
        IssueBreakglassOfflineCodesRequest: {
            identity: string;
            allowedPrincipals: string[];
            nodeIds?: string[];
            /** @description Optional source-CIDR binding (deny-only reducer). */
            sourceCidr?: string;
            /**
             * Format: int32
             * @description How many codes to issue (default 10).
             */
            count?: number;
            /**
             * Format: int64
             * @description Code lifetime (default 90d).
             */
            ttlSeconds?: number;
        };
        /** Issued Break-glass Offline Codes */
        IssuedBreakglassOfflineCodes: {
            ids: string[];
            /** @description The raw offline codes, returned ONCE; only their hashes are stored. */
            codes: string[];
            /** Format: date-time */
            expiresAt: string;
        };
        /**
         * Break-glass Offline Code
         * @description Offline-code metadata (never the raw code).
         */
        BreakglassOfflineCodeResource: {
            /** Format: uuid */
            id: string;
            identity: string;
            allowedPrincipals?: string[];
            sourceCidr?: string;
            /** Format: date-time */
            expiresAt: string;
            used: boolean;
            /** Format: date-time */
            usedAt?: string;
            /** Format: date-time */
            revokedAt?: string;
            createdBy?: string;
            /** Format: date-time */
            createdAt?: string;
        };
        /** Break-glass Offline Code List */
        BreakglassOfflineCodeList: {
            offlineCodes: components["schemas"]["BreakglassOfflineCodeResource"][];
        };
        /** Break-glass Activation */
        BreakglassActivationResource: {
            /** Format: uuid */
            id: string;
            identity?: string;
            principal: string;
            reason: string;
            alertRef?: string;
            breakglassPolicyName?: string;
            /** @enum {string} */
            reviewStatus: "pending" | "reviewed";
            reviewer?: string;
            sourceIp?: string;
            /** Format: uuid */
            targetNodeId?: string;
            credentialRef?: string;
            /** Format: date-time */
            activatedAt: string;
            /** Format: date-time */
            reviewedAt?: string;
        };
        /** Break-glass Activation List */
        BreakglassActivationList: {
            activations: components["schemas"]["BreakglassActivationResource"][];
        };
        /** Review Break-glass Activation Request */
        ReviewBreakglassActivationRequest: {
            note?: string;
        };
        /**
         * Config Provenance
         * @description Provenance of a config row (Design §13). `api`/`ui` mark which admin surface
         *     last wrote it; `default` is a seeded/cold-start default. Read-only.
         * @enum {string}
         */
        Origin: "api" | "ui" | "default";
        /**
         * SSH Capability
         * @description A policy-gated SSH channel capability (Design D14).
         * @enum {string}
         */
        Capability: "shell" | "exec" | "sftp" | "scp" | "port_forward_local" | "port_forward_remote" | "agent_forward" | "x11";
        /**
         * Platform Permission
         * @description A granular platform-RBAC permission from the closed vocabulary (FR-PADM-1).
         * @enum {string}
         */
        PlatformPermission: "rbac:read" | "rbac:write" | "node:enroll" | "node:quarantine" | "node:remove" | "gateway:enroll" | "gateway:remove" | "ca:manage" | "ca:rotate" | "request:approve" | "recording:replay" | "recording:export" | "recording:delete" | "recording:key-manage" | "audit:read" | "user:manage" | "settings:write" | "lock:read" | "lock:write" | "breakglass:manage";
        /**
         * Rule Effect
         * @enum {string}
         */
        Effect: "allow" | "deny";
        /**
         * Connector Kind
         * @enum {string}
         */
        ConnectorKind: "agent" | "agentless";
        /**
         * Binding Subject Kind
         * @enum {string}
         */
        SubjectKind: "user" | "group";
        /**
         * CA Kind
         * @enum {string}
         */
        CaKind: "user" | "session" | "host";
        /**
         * CA Backend
         * @description Where a CA's private key lives. Two of these can actually sign:
         *
         *     - `local` — the key is in the Control Plane's database, encrypted under an
         *       operator KEK. Always available.
         *     - `azure_keyvault` — the key is in Azure Key Vault and never leaves it. The
         *       Control Plane signs by calling the vault, and the deployment must have
         *       `sessionlayer.ca.azure.*` configured; a CA naming this backend on a
         *       Control Plane that does not fails closed rather than signing locally.
         *
         *     `aws_kms` and `vault` are **integration seams with no implementation**: the
         *     interfaces exist for a deployment to bind, but this build cannot produce a
         *     signer for either, so a CA naming one would accept the write and then fail
         *     every signature. Creating or updating a CA onto them is rejected (`422`).
         *
         *     The set is deliberately never narrowed — a row an upgraded deployment
         *     already holds must stay readable.
         * @enum {string}
         */
        CaBackend: "local" | "aws_kms" | "azure_keyvault" | "vault";
        /**
         * CA Algorithm
         * @description The algorithms a CA row may carry. This set is deliberately EXACTLY the
         *     set the `ca_config.algorithm` CHECK admits, so no stored row can hold a
         *     value this enum cannot map — the read paths deserialize the stored
         *     algorithm through this enum, and a value outside it would fail the read
         *     rather than the write.
         *
         *     The set is therefore wider than what can be created. Only the three
         *     ECDSA curves can actually be assembled into a signer; a create naming
         *     one of the others is rejected with a `422` that says so. They remain
         *     members because a deployment may already hold such a row from before
         *     that check existed, and `GET /v1/cas` has to be able to show it.
         * @enum {string}
         */
        CaAlgorithm: "ecdsa-p256" | "ecdsa-p384" | "ecdsa-p521" | "ed25519" | "rsa-2048" | "rsa-4096";
        /**
         * CA Rotation State
         * @description Rotation-state-machine position (managed server-side; read-only over the API).
         * @enum {string}
         */
        CaRotationState: "incoming" | "active" | "outgoing" | "expired";
        /**
         * Service-Account Auth Method
         * @enum {string}
         */
        ServiceAccountAuthMethod: "private_key_jwt" | "mtls" | "client_secret";
        /**
         * Break-glass Auth Path
         * @enum {string}
         */
        BreakglassAuthPath: "fido2" | "offline_code";
        /**
         * Access Model
         * @enum {string}
         */
        AccessModel: "standing" | "jit" | "breakglass";
        /**
         * WORM Mode
         * @description Object-lock mode for new recordings (FR-AUD-3/6). `governance` is deletable by a privileged audited role (the GDPR escape hatch); `compliance` is truly un-deletable. The cluster default only ratchets towards `compliance`.
         * @enum {string}
         */
        WormMode: "compliance" | "governance";
        /**
         * Recording Key Seal Algorithm
         * @description How the Gateway seals a per-recording data key to the customer's public key. `ecies_p256` (the portable default) requires an EC key on P-256; `rsa_oaep_sha256` requires an RSA key. A submitted key that does not match is rejected (`422`).
         * @enum {string}
         */
        RecordingKeySealAlgorithm: "ecies_p256" | "rsa_oaep_sha256";
        /**
         * Deployment-Managed Setting
         * @description An operator-settings field currently pinned by a deployment property and reconciled into the row on every boot. Changing a pinned field over the API is refused (`422`) rather than accepted and silently reverted at the next restart.
         * @enum {string}
         */
        DeploymentManagedField: "defaultMaxSessionSeconds" | "defaultIdleTimeoutSeconds" | "defaultMaxConcurrentSessions";
        /**
         * Selector
         * @description A shape-validated selector object (stored as jsonb).
         */
        Selector: {
            [key: string]: unknown;
        };
        /**
         * Label Map
         * @description A string->string label map.
         */
        LabelMap: {
            [key: string]: string;
        };
        /** Data-Plane Rule */
        RuleResource: {
            /** Format: uuid */
            id: string;
            name: string;
            identitySelector: components["schemas"]["Selector"];
            nodeLabelSelector: components["schemas"]["Selector"];
            sourceIpCondition?: components["schemas"]["Selector"];
            principals: string[];
            /** Format: int32 */
            ttlSeconds: number;
            capabilities: components["schemas"]["Capability"][];
            effect: components["schemas"]["Effect"];
            origin: components["schemas"]["Origin"];
            /** Format: int64 */
            version: number;
            /** Format: date-time */
            createdAt?: string;
            /** Format: date-time */
            updatedAt?: string;
        };
        /** Create Rule Request */
        CreateRuleRequest: {
            name: string;
            identitySelector: components["schemas"]["Selector"];
            nodeLabelSelector: components["schemas"]["Selector"];
            sourceIpCondition?: components["schemas"]["Selector"];
            principals: string[];
            /** Format: int32 */
            ttlSeconds: number;
            capabilities?: components["schemas"]["Capability"][];
            effect: components["schemas"]["Effect"];
        };
        /** Update Rule Request */
        UpdateRuleRequest: {
            identitySelector: components["schemas"]["Selector"];
            nodeLabelSelector: components["schemas"]["Selector"];
            sourceIpCondition?: components["schemas"]["Selector"];
            principals: string[];
            /** Format: int32 */
            ttlSeconds: number;
            capabilities?: components["schemas"]["Capability"][];
            effect: components["schemas"]["Effect"];
            /**
             * Format: int64
             * @description The current resource version; required and must match (optimistic concurrency — no silent lost update).
             */
            version: number;
        };
        /** Rule Page */
        RulePage: {
            items: components["schemas"]["RuleResource"][];
            /** @description Cursor for the next page; absent on the last page. */
            nextCursor?: string;
        };
        /** Platform Role */
        RoleResource: {
            /** Format: uuid */
            id: string;
            name: string;
            permissions: components["schemas"]["PlatformPermission"][];
            description?: string;
            origin: components["schemas"]["Origin"];
            /** Format: int64 */
            version: number;
            /** Format: date-time */
            createdAt?: string;
            /** Format: date-time */
            updatedAt?: string;
        };
        /** Create Role Request */
        CreateRoleRequest: {
            name: string;
            permissions: components["schemas"]["PlatformPermission"][];
            description?: string;
        };
        /** Update Role Request */
        UpdateRoleRequest: {
            permissions: components["schemas"]["PlatformPermission"][];
            description?: string;
            /** Format: int64 */
            version: number;
        };
        /** Role Page */
        RolePage: {
            items: components["schemas"]["RoleResource"][];
            nextCursor?: string;
        };
        /** Role Binding */
        RoleBindingResource: {
            /** Format: uuid */
            id: string;
            /** Format: uuid */
            roleId: string;
            subjectKind: components["schemas"]["SubjectKind"];
            subject: string;
            scope?: components["schemas"]["Selector"];
            origin: components["schemas"]["Origin"];
            /** Format: int64 */
            version: number;
            /** Format: date-time */
            createdAt?: string;
            /** Format: date-time */
            updatedAt?: string;
        };
        /** Create Role Binding Request */
        CreateRoleBindingRequest: {
            /** Format: uuid */
            roleId: string;
            subjectKind: components["schemas"]["SubjectKind"];
            subject: string;
            scope?: components["schemas"]["Selector"];
        };
        /** Update Role Binding Request */
        UpdateRoleBindingRequest: {
            scope?: components["schemas"]["Selector"];
            /** Format: int64 */
            version: number;
        };
        /** Role Binding Page */
        RoleBindingPage: {
            items: components["schemas"]["RoleBindingResource"][];
            nextCursor?: string;
        };
        /**
         * CA Configuration
         * @description Per-CA backend + key REFERENCE. NEVER contains private key material.
         */
        CaResource: {
            /** Format: uuid */
            id: string;
            name: string;
            caKind: components["schemas"]["CaKind"];
            backend: components["schemas"]["CaBackend"];
            /** @description A backend key handle/reference only — NEVER private key material. */
            keyReference: string;
            algorithm: components["schemas"]["CaAlgorithm"];
            rotationState: components["schemas"]["CaRotationState"];
            origin: components["schemas"]["Origin"];
            /** Format: int64 */
            version: number;
            /** Format: date-time */
            createdAt?: string;
            /** Format: date-time */
            updatedAt?: string;
        };
        /** Create CA Request */
        CreateCaRequest: {
            name: string;
            caKind: components["schemas"]["CaKind"];
            backend: components["schemas"]["CaBackend"];
            /** @description A backend key handle/reference only — private material is rejected (422). */
            keyReference: string;
            algorithm?: components["schemas"]["CaAlgorithm"];
        };
        /** Update CA Request */
        UpdateCaRequest: {
            backend: components["schemas"]["CaBackend"];
            keyReference: string;
            algorithm: components["schemas"]["CaAlgorithm"];
            /** Format: int64 */
            version: number;
        };
        /**
         * Rotate CA Request
         * @description Optional overrides for the incoming key; omitted fields inherit the active CA.
         *
         *     Overriding `backend` is how a CA is **moved onto a different key service**.
         *     A CA's private key cannot be migrated — a key in Azure Key Vault is *in*
         *     Key Vault — so adoption is necessarily a rotation onto a new key, and the
         *     overlap window is what gives the fleet time to trust it.
         */
        RotateCaRequest: {
            algorithm?: components["schemas"]["CaAlgorithm"];
            /**
             * @description The backend the incoming key lives in; defaults to the active CA's.
             *     Naming a backend this build has no signer for is a `422` — the check
             *     happens before anything is written, so a rotation cannot leave the CA
             *     kind pointing at a key nothing can sign with.
             *
             *     When this is `azure_keyvault`, `keyReference` is required and must be a
             *     fully **versioned** Key Vault key identifier.
             */
            backend?: components["schemas"]["CaBackend"];
            /**
             * @description The incoming key handle/reference (backend-provisioned); private
             *     material is rejected (`422`).
             *
             *     For `azure_keyvault` this is the versioned key identifier
             *     `https://<vault>.vault.azure.net/keys/<name>/<version>`. The version is
             *     **required**: the CA's public half is published to every node's
             *     trusted-CA set, so a vault-side rotation that floated the signing key
             *     underneath a running CA would have the Control Plane emitting
             *     certificates no node trusts, with no error on the Control Plane side.
             */
            keyReference?: string;
        };
        /** CA Page */
        CaPage: {
            items: components["schemas"]["CaResource"][];
            nextCursor?: string;
        };
        /**
         * CA Public Key
         * @description The active CA's PUBLIC key for one SSH kind — the material a node or a client pins to verify certificates this CA issues. No private key, key reference, or wrapped material appears here or can be derived from it.
         */
        CaPublicKey: {
            caKind: components["schemas"]["CaKind"];
            algorithm: components["schemas"]["CaAlgorithm"];
            rotationState: components["schemas"]["CaRotationState"];
            /** @description The public key as base64-encoded DER SubjectPublicKeyInfo. */
            publicKeySpkiDer: string;
            /** @description The same key as a single OpenSSH authorized-key line (`ecdsa-sha2-nistp256 AAAA...`), ready to write into `TrustedUserCAKeys` or a `@cert-authority` entry. */
            opensshPublicKey: string;
            /** @description The OpenSSH `SHA256:...` fingerprint of `opensshPublicKey`, as `ssh-keygen -l` prints it, so an operator can verify what they installed matches what the Control Plane serves. */
            fingerprint: string;
        };
        /**
         * Service Account
         * @description A machine-consumer DEFINITION. NEVER contains a secret.
         */
        ServiceAccountResource: {
            /** Format: uuid */
            id: string;
            name: string;
            description?: string;
            authMethod: components["schemas"]["ServiceAccountAuthMethod"];
            /** @description A public key / JWKS reference only — NEVER a secret. */
            keyReference?: string;
            /** Format: int32 */
            tokenTtlSeconds?: number;
            origin: components["schemas"]["Origin"];
            /** Format: int64 */
            version: number;
            /** Format: date-time */
            createdAt?: string;
            /** Format: date-time */
            updatedAt?: string;
        };
        /** Create Service Account Request */
        CreateServiceAccountRequest: {
            name: string;
            description?: string;
            authMethod?: components["schemas"]["ServiceAccountAuthMethod"];
            /** @description A public key / JWKS reference only — private material is rejected (422). */
            keyReference?: string;
            /** Format: int32 */
            tokenTtlSeconds?: number;
        };
        /** Update Service Account Request */
        UpdateServiceAccountRequest: {
            description?: string;
            authMethod?: components["schemas"]["ServiceAccountAuthMethod"];
            keyReference?: string;
            /** Format: int32 */
            tokenTtlSeconds?: number;
            /** Format: int64 */
            version: number;
        };
        /** Service Account Page */
        ServiceAccountPage: {
            items: components["schemas"]["ServiceAccountResource"][];
            nextCursor?: string;
        };
        /** Node Policy */
        NodePolicyResource: {
            /** Format: uuid */
            id: string;
            name: string;
            desiredLabels: components["schemas"]["LabelMap"];
            connectorKind: components["schemas"]["ConnectorKind"];
            hostPinRef?: string;
            hostCaRef?: string;
            origin: components["schemas"]["Origin"];
            /** Format: int64 */
            version: number;
            /** Format: date-time */
            createdAt?: string;
            /** Format: date-time */
            updatedAt?: string;
        };
        /** Create Node Policy Request */
        CreateNodePolicyRequest: {
            name: string;
            desiredLabels?: components["schemas"]["LabelMap"];
            connectorKind: components["schemas"]["ConnectorKind"];
            hostPinRef?: string;
            hostCaRef?: string;
        };
        /** Update Node Policy Request */
        UpdateNodePolicyRequest: {
            desiredLabels?: components["schemas"]["LabelMap"];
            connectorKind: components["schemas"]["ConnectorKind"];
            hostPinRef?: string;
            hostCaRef?: string;
            /** Format: int64 */
            version: number;
        };
        /** Node Policy Page */
        NodePolicyPage: {
            items: components["schemas"]["NodePolicyResource"][];
            nextCursor?: string;
        };
        /** Capability Definition */
        CapabilityDefResource: {
            /** Format: uuid */
            id: string;
            name: components["schemas"]["Capability"];
            description?: string;
            origin: components["schemas"]["Origin"];
            /** Format: int64 */
            version: number;
            /** Format: date-time */
            createdAt?: string;
            /** Format: date-time */
            updatedAt?: string;
        };
        /** Create Capability Definition Request */
        CreateCapabilityDefRequest: {
            name: components["schemas"]["Capability"];
            description?: string;
        };
        /** Update Capability Definition Request */
        UpdateCapabilityDefRequest: {
            description?: string;
            /** Format: int64 */
            version: number;
        };
        /** Capability Definition Page */
        CapabilityDefPage: {
            items: components["schemas"]["CapabilityDefResource"][];
            nextCursor?: string;
        };
        /** JIT Policy */
        JitPolicyResource: {
            /** Format: uuid */
            id: string;
            name: string;
            targetSelector: components["schemas"]["Selector"];
            capabilities: components["schemas"]["Capability"][];
            /** Format: int32 */
            maxTtlSeconds: number;
            approvalChain: components["schemas"]["JitApprovalLevel"][];
            origin: components["schemas"]["Origin"];
            /** Format: int64 */
            version: number;
            /** Format: date-time */
            createdAt?: string;
            /** Format: date-time */
            updatedAt?: string;
        };
        /** Create JIT Policy Request */
        CreateJitPolicyRequest: {
            name: string;
            targetSelector: components["schemas"]["Selector"];
            capabilities?: components["schemas"]["Capability"][];
            /** Format: int32 */
            maxTtlSeconds: number;
            approvalChain?: components["schemas"]["JitApprovalLevel"][];
        };
        /** Update JIT Policy Request */
        UpdateJitPolicyRequest: {
            targetSelector: components["schemas"]["Selector"];
            capabilities?: components["schemas"]["Capability"][];
            /** Format: int32 */
            maxTtlSeconds: number;
            approvalChain?: components["schemas"]["JitApprovalLevel"][];
            /** Format: int64 */
            version: number;
        };
        /** JIT Policy Page */
        JitPolicyPage: {
            items: components["schemas"]["JitPolicyResource"][];
            nextCursor?: string;
        };
        /** Break-glass Policy */
        BreakglassPolicyResource: {
            /** Format: uuid */
            id: string;
            name: string;
            recordingStrict: boolean;
            alertTarget: string;
            reviewRequired: boolean;
            authPath: components["schemas"]["BreakglassAuthPath"];
            origin: components["schemas"]["Origin"];
            /** Format: int64 */
            version: number;
            /** Format: date-time */
            createdAt?: string;
            /** Format: date-time */
            updatedAt?: string;
        };
        /** Create Break-glass Policy Request */
        CreateBreakglassPolicyRequest: {
            name: string;
            recordingStrict?: boolean;
            alertTarget: string;
            reviewRequired?: boolean;
            authPath?: components["schemas"]["BreakglassAuthPath"];
        };
        /** Update Break-glass Policy Request */
        UpdateBreakglassPolicyRequest: {
            recordingStrict?: boolean;
            alertTarget: string;
            reviewRequired?: boolean;
            authPath?: components["schemas"]["BreakglassAuthPath"];
            /** Format: int64 */
            version: number;
        };
        /** Break-glass Policy Page */
        BreakglassPolicyPage: {
            items: components["schemas"]["BreakglassPolicyResource"][];
            nextCursor?: string;
        };
        /**
         * Session-Limit Policy
         * @description A per-identity override for the FR-SESS-3 session-limit knobs. Every
         *     stored value is enforced: `maxConcurrentSessions` as the HARD cap at
         *     Authorize, `maxSessionSeconds` folded into the decision's `grant_expiry`,
         *     `idleTimeoutSeconds` signed into the decision context and applied
         *     (tighten-only) by the Gateway. An absent knob defers to the
         *     `operator_settings` cluster default for that knob.
         */
        SessionLimitPolicyResource: {
            /** Format: uuid */
            id: string;
            name: string;
            identitySelector: components["schemas"]["Selector"];
            /** Format: int32 */
            maxConcurrentSessions?: number;
            /** Format: int32 */
            maxSessionSeconds?: number;
            /** Format: int32 */
            idleTimeoutSeconds?: number;
            origin: components["schemas"]["Origin"];
            /** Format: int64 */
            version: number;
            /** Format: date-time */
            createdAt?: string;
            /** Format: date-time */
            updatedAt?: string;
        };
        /** Create Session-Limit Policy Request */
        CreateSessionLimitPolicyRequest: {
            name: string;
            identitySelector: components["schemas"]["Selector"];
            /** Format: int32 */
            maxConcurrentSessions?: number;
            /** Format: int32 */
            maxSessionSeconds?: number;
            /** Format: int32 */
            idleTimeoutSeconds?: number;
        };
        /** Update Session-Limit Policy Request */
        UpdateSessionLimitPolicyRequest: {
            identitySelector: components["schemas"]["Selector"];
            /** Format: int32 */
            maxConcurrentSessions?: number;
            /** Format: int32 */
            maxSessionSeconds?: number;
            /** Format: int32 */
            idleTimeoutSeconds?: number;
            /**
             * Format: int64
             * @description The current resource version; required and must match (optimistic concurrency — no silent lost update).
             */
            version: number;
        };
        /** Session-Limit Policy Page */
        SessionLimitPolicyPage: {
            items: components["schemas"]["SessionLimitPolicyResource"][];
            /** @description Cursor for the next page; absent on the last page. */
            nextCursor?: string;
        };
        /**
         * Operator Settings
         * @description The cluster-wide operator settings singleton. This is a deliberately
         *     narrower projection than the underlying row: the key-encryption-key
         *     reference, the first-admin bootstrap fields, and the recording key's
         *     bytes are not part of it and cannot be derived from it.
         *
         *     `defaultCaBackend`, `origin`, `version` and the timestamps are read-only
         *     — `defaultCaBackend` is consumed only when cold start provisions a CA
         *     kind that has no row yet, so it governs nothing once the CAs exist; a
         *     live CA's backend is managed under `/v1/cas`.
         */
        OperatorSettings: {
            /**
             * Format: int32
             * @description How long audit-event partitions are kept (FR-AUD-6). Increases only over this API.
             */
            auditRetentionDays: number;
            /**
             * Format: int32
             * @description The retention window stamped on new recordings as the object-lock retain-until (FR-AUD-6). Increases only over this API.
             */
            recordingRetentionDays: number;
            defaultWormMode: components["schemas"]["WormMode"];
            /**
             * Format: int32
             * @description Default OTP lifetime (FR-AUTH-9).
             */
            otpTtlSeconds: number;
            defaultCaBackend: components["schemas"]["CaBackend"];
            /**
             * Format: int32
             * @description Cluster default max session duration (FR-SESS-3); absent means no cluster-wide ceiling. Per-identity overrides live in `/v1/session-limit-policies`.
             */
            defaultMaxSessionSeconds?: number;
            /**
             * Format: int32
             * @description Cluster default idle timeout (FR-SESS-3); absent means none.
             */
            defaultIdleTimeoutSeconds?: number;
            /**
             * Format: int32
             * @description Cluster default concurrent-session cap (FR-SESS-3); absent means uncapped by default.
             */
            defaultMaxConcurrentSessions?: number;
            /** @description Which fields a deployment property currently pins. Computed per request, so it reflects this Control Plane's configuration rather than anything stored. A write to a listed field is a `422`. */
            deploymentManagedFields: components["schemas"]["DeploymentManagedField"][];
            /** @description Whether a customer recording key is provisioned. False means strict recording will refuse every session until one is set; the key itself is served by `/v1/operator-settings/recording-customer-key`. */
            recordingKeyConfigured: boolean;
            recordingKeySealAlgorithm: components["schemas"]["RecordingKeySealAlgorithm"];
            /** @description The operator's reference to the customer key — NEVER key material. */
            recordingKeyRef?: string;
            origin: components["schemas"]["Origin"];
            /** Format: int64 */
            version: number;
            /** Format: date-time */
            createdAt?: string;
            /** Format: date-time */
            updatedAt?: string;
        };
        /**
         * Update Operator Settings Request
         * @description Replaces the writable settings. The four always-present knobs are
         *     required, so a partial body can never quietly reset one to its
         *     cold-start default. The three session-limit defaults are nullable in the
         *     model, and omitting one CLEARS the cluster default for that knob — read,
         *     modify, and send the whole resource back.
         *
         *     Both retention fields and `defaultWormMode` ratchet: they accept an
         *     unchanged value or a move towards keeping more evidence
         *     (`governance` -> `compliance`), and refuse the other direction with a
         *     `422`.
         *
         *     `defaultCaBackend`, the key-encryption-key reference, the bootstrap
         *     fields and the recording key are absent by design; sending any of them
         *     is a `400`, not a silent no-op.
         */
        UpdateOperatorSettingsRequest: {
            /**
             * Format: int32
             * @description Must be greater than or equal to the stored value; a decrease is a `422`.
             */
            auditRetentionDays: number;
            /**
             * Format: int32
             * @description Must be greater than or equal to the stored value; a decrease is a `422`.
             */
            recordingRetentionDays: number;
            defaultWormMode: components["schemas"]["WormMode"];
            /** Format: int32 */
            otpTtlSeconds: number;
            /**
             * Format: int32
             * @description Omit to clear. A change to a deployment-pinned field is a `422`.
             */
            defaultMaxSessionSeconds?: number;
            /**
             * Format: int32
             * @description Omit to clear. A change to a deployment-pinned field is a `422`.
             */
            defaultIdleTimeoutSeconds?: number;
            /**
             * Format: int32
             * @description Omit to clear. A change to a deployment-pinned field is a `422`.
             */
            defaultMaxConcurrentSessions?: number;
            /**
             * Format: int64
             * @description The current resource version; required and must match (optimistic concurrency — no silent lost update).
             */
            version: number;
        };
        /**
         * Customer Recording Key
         * @description The customer-held recording key, PUBLIC half only (FR-AUD-2, Design §15). The private half never reaches the platform, so the platform cannot decrypt recordings sealed to it.
         */
        RecordingCustomerKey: {
            /** @description False on a fresh install, where no key has been provisioned yet and strict recording therefore refuses every session. The key fields below are absent in that state. */
            configured: boolean;
            /** @description The customer's public key as base64-encoded DER SubjectPublicKeyInfo — the exact text that was submitted, so an operator can compare it against their own record of what they provisioned. */
            publicKey?: string;
            sealAlgorithm: components["schemas"]["RecordingKeySealAlgorithm"];
            /** @description The operator's reference to the key (persisted onto each recording so replay can find the customer's unwrap path) — NEVER key material. */
            keyRef?: string;
            /** @description Lowercase hex SHA-256 over the DER SubjectPublicKeyInfo. This is the value a rotation must echo, and the only form of the key that audit records carry. */
            fingerprintSha256?: string;
            /**
             * Format: date-time
             * @description When the key was last provisioned or rotated.
             */
            updatedAt?: string;
        };
        /**
         * Set Recording Customer Key Request
         * @description Provisions or rotates the customer recording key. Only the PUBLIC half is
         *     ever accepted: a submission that is private key material in any encoding
         *     is rejected outright (`422`), as is one whose curve or algorithm does not
         *     match `sealAlgorithm`.
         *
         *     A write when a key is already configured is a ROTATION, and every
         *     recording sealed under the outgoing key stays readable only by the
         *     outgoing private key. A rotation therefore additionally requires
         *     `expectedFingerprintSha256` and
         *     `acknowledgeExistingRecordingsUndecryptable`. First provisioning
         *     requires neither, and refuses both with a `422`: a caller that believes
         *     it is replacing a key when none exists has lost track of the cluster's
         *     state, and silently provisioning for it would hide that.
         */
        SetRecordingCustomerKeyRequest: {
            /** @description The customer's PUBLIC key as base64-encoded DER SubjectPublicKeyInfo, at most 8 KiB decoded. Private key material — PKCS#8, SEC1, or PEM text of any kind — is rejected (`422`); only the public half is ever stored. */
            publicKey: string;
            sealAlgorithm: components["schemas"]["RecordingKeySealAlgorithm"];
            /** @description An optional operator reference to where the private half is held. A reference only — a value carrying key material is a `422`. */
            keyRef?: string;
            /** @description ROTATION ONLY, and required there: the lowercase hex SHA-256 of the key being replaced. A mismatch is a `409`, so a blind or racing overwrite of a key someone else just set cannot succeed. */
            expectedFingerprintSha256?: string;
            /** @description ROTATION ONLY, and required there: must be `true`, acknowledging that recordings sealed under the outgoing key will not be readable with the incoming one. Absent or `false` is a `422`. */
            acknowledgeExistingRecordingsUndecryptable?: boolean;
            /**
             * Format: int64
             * @description The current operator-settings version; required and must match (optimistic concurrency — a stale version is a `409`).
             */
            version: number;
        };
        /** SSH Session */
        SessionResource: {
            /** Format: uuid */
            id: string;
            identity: string;
            /** Format: uuid */
            nodeId?: string;
            nodeName?: string;
            principal: string;
            /** Format: uuid */
            gatewayId?: string;
            gatewayName?: string;
            accessModel: components["schemas"]["AccessModel"];
            capabilities: components["schemas"]["Capability"][];
            /** Format: uuid */
            matchedRuleId?: string;
            matchedRuleName?: string;
            /** Format: uuid */
            jitRequestId?: string;
            /** Format: uuid */
            breakglassActivationId?: string;
            /** Format: int64 */
            policyEpoch?: number;
            /** Format: date-time */
            grantExpiry?: string;
            /** Format: date-time */
            startedAt: string;
            /** Format: date-time */
            endedAt?: string;
            endReason?: string;
        };
        /** Terminate Session Request */
        TerminateSessionRequest: {
            /**
             * @description Operator/audit reason (never disclosed to the SSH user). Bounded like a
             *     Lock reason (it becomes one) so it cannot inflate the pushed deny-list.
             */
            reason?: string;
        };
        /** Session Page */
        SessionPage: {
            items: components["schemas"]["SessionResource"][];
            nextCursor?: string;
        };
        /**
         * Concurrency Lease
         * @description One FR-SESS-3 concurrency lease. The per-identity cap counts the leases that are unreleased AND unexpired, so a lease outliving its session keeps occupying a slot until it is released or its safety TTL passes.
         */
        SessionLeaseResource: {
            /** Format: uuid */
            id: string;
            /** @description The subject identity the cap is enforced per. */
            identity: string;
            /**
             * Format: uuid
             * @description The session that took the lease; absent if that session's row has since been pruned, which is itself a sign of a stuck lease.
             */
            sessionId?: string;
            /** @description The Gateway that took the lease, by NAME. A lease held by a Gateway that no longer exists cannot be released by its owner and is the usual reason one gets stuck. */
            gatewayName?: string;
            /** Format: date-time */
            acquiredAt: string;
            /**
             * Format: date-time
             * @description The lease safety TTL. Once it passes, the lease stops counting immediately and the reaper later stamps `releasedAt` to tidy the row. ABSENT means no TTL — the lease counts until something releases it, which is the only unbounded case.
             */
            expiresAt?: string;
            /**
             * Format: date-time
             * @description When the lease was released, by a session ending, by an operator, or by the reaper. Absent while unreleased — which is not the same as counting, since an expired lease is unreleased and uncounted.
             */
            releasedAt?: string;
            /** @description Whether this lease is counted right now by the cap query, which evaluates unreleased AND (no expiry OR expiry in the future). An expired-but-unreleased lease does NOT count — it has already stopped occupying the slot, and `releasedAt` merely catches up later. */
            countsTowardCap: boolean;
        };
        /** Release Session Lease Request */
        ReleaseSessionLeaseRequest: {
            /**
             * @description Operator/audit reason, REQUIRED. Releasing a lease corrects live
             *     enforcement state, and an unexplained release is indistinguishable
             *     afterwards from an operator quietly granting somebody extra capacity.
             */
            reason: string;
        };
        /** Session Lease Page */
        SessionLeasePage: {
            items: components["schemas"]["SessionLeaseResource"][];
            /** @description Cursor for the next page; absent on the last page. */
            nextCursor?: string;
        };
        /**
         * Recording
         * @description Session recording metadata (Design §12). NEVER carries recording bytes.
         */
        RecordingResource: {
            /** Format: uuid */
            id: string;
            /** Format: uuid */
            sessionId: string;
            /** @description The session subject identity (the recorded user). */
            identity?: string;
            /** Format: uuid */
            nodeId?: string;
            /** @description Capture format (e.g. `asciicast-v2`). */
            format?: string;
            /** @description Lifecycle status (`recording|finalized|truncated|failed`). */
            status?: string;
            /** @description WORM object-lock mode (`compliance|governance`). */
            wormMode?: string;
            /** Format: int64 */
            sizeBytes?: number;
            /** @description The tamper-evidence hash-chain head (hex). */
            hashChainHead?: string;
            /** @description A reference to the customer-held encryption key — NEVER key material. */
            encryptionKeyRef?: string;
            legalHold?: boolean;
            /** Format: date-time */
            retentionUntil?: string;
            /**
             * Format: date-time
             * @description When the encrypted object was deleted (retention prune or governance delete); metadata is retained.
             */
            prunedAt?: string;
            /** Format: date-time */
            startedAt?: string;
            /** Format: date-time */
            endedAt?: string;
            /** Format: date-time */
            createdAt?: string;
        };
        /**
         * Legal Hold Request
         * @description Places or releases a legal hold on a recording (FR-AUD-6).
         */
        LegalHoldRequest: {
            /** @description True to place a legal hold, false to release it. */
            held: boolean;
            reason?: string;
        };
        /** Recording Page */
        RecordingPage: {
            items: components["schemas"]["RecordingResource"][];
            nextCursor?: string;
        };
        /**
         * Signed URL
         * @description A short-lived, single-object signed URL. Bytes never proxy through the CP.
         */
        SignedUrl: {
            /** Format: uri */
            url: string;
            /** @description The HTTP method to use against the signed URL (e.g. `GET`). */
            method: string;
            /** Format: date-time */
            expiresAt: string;
        };
        /**
         * Audit Event
         * @description One row of the correlated append-only audit stream (Design §12).
         *
         *     `seq`, `prevHash` and `recordHash` are the tamper-evidence chain, and
         *     they are present ONLY for an unscoped `audit:read` binding. A
         *     scope-filtered reader gets the row without them, because `seq` is a dense
         *     global ordinal: a reader who sees `seq` 1000 then 1007 has learned that
         *     six events exist that they are not entitled to see, and the linkage would
         *     not resolve for them anyway. Their absence is therefore normal, not an
         *     error — treat all three as optional.
         */
        AuditEventResource: {
            /** Format: uuid */
            id: string;
            /** Format: date-time */
            occurredAt: string;
            actor: string;
            subject?: string;
            action: string;
            outcome: string;
            /** Format: uuid */
            sessionId?: string;
            /** Format: uuid */
            nodeId?: string;
            /** Format: uuid */
            correlationId?: string;
            sourceIp?: string;
            /** @description The session's capability snapshot (FR-AUD-8 search dimension). */
            capabilities?: string[];
            accessModel?: components["schemas"]["AccessModel"];
            nodeLabels?: components["schemas"]["LabelMap"];
            detail?: components["schemas"]["Selector"];
            /**
             * Format: int64
             * @description Dense chain ordinal assigned by the datastore. Gaps across a page mean rows are missing from the stream. Unscoped `audit:read` only.
             */
            seq?: number;
            /** @description The preceding row's `recordHash`, or 64 zeroes at the genesis row. Walking `prevHash[n+1] == recordHash[n]` proves the returned rows are contiguous and unreordered. Unscoped `audit:read` only. */
            prevHash?: string;
            /** @description This row's chain hash, lowercase hex. Recomputing it independently requires reproducing the server's canonical encoding of the event byte-for-byte, which this contract does not specify; over the API the reliable checks are linkage and `seq` density. Unscoped `audit:read` only. */
            recordHash?: string;
        };
        /** Audit Event Page */
        AuditEventPage: {
            items: components["schemas"]["AuditEventResource"][];
            nextCursor?: string;
        };
    };
    responses: {
        /** @description An error occurred. The body is an RFC 9457 problem document. */
        Problem: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["ProblemDetails"];
            };
        };
    };
    parameters: {
        /**
         * @description Opaque forward-only pagination cursor from a previous page's `nextCursor`.
         *     Omit for the first page. An unrecognised cursor is a `400`.
         */
        Cursor: string;
        /** @description Maximum items to return in one page (the server clamps to its maximum). */
        Limit: number;
        /**
         * @description Optional client-generated idempotency key scoping a mutating request. A
         *     retry with the same key, method, and path returns the original response and
         *     never repeats the side effect; the same key with a different request body is
         *     a `422`. Keys are retained for a bounded TTL.
         */
        IdempotencyKey: string;
        RuleId: string;
        RoleId: string;
        BindingId: string;
        CaId: string;
        /** @description Which SSH CA to address. The closed set is the three SSH kinds; the internal mTLS CA is not a member and any other value is a `400`. */
        CaKindPath: components["schemas"]["CaKind"];
        ServiceAccountId: string;
        NodePolicyId: string;
        CapabilityDefId: string;
        JitPolicyId: string;
        BreakglassPolicyId: string;
        SessionLimitPolicyId: string;
        GatewayId: string;
        SessionId: string;
        SessionLeaseId: string;
        RecordingId: string;
        AuditEventId: string;
    };
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    getHealth: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The Control Plane is healthy. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HealthStatus"];
                };
            };
            /** @description The Control Plane is not ready to serve requests. */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["ProblemDetails"];
                };
            };
            "4XX": components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getVersion: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Version metadata. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["VersionInfo"];
                };
            };
            "4XX": components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    claimBootstrap: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["BootstrapClaimRequest"];
            };
        };
        responses: {
            /** @description The credential was valid; the caller is now the first admin. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BootstrapClaimResult"];
                };
            };
            /** @description The credential did not match the armed hash. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BootstrapClaimResult"];
                };
            };
            /** @description Bootstrap already completed (by this or a concurrent claim), or no credential is currently armed (a config-named admin subject was used instead, or the credential was never printed). */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BootstrapClaimResult"];
                };
            };
            default: components["responses"]["Problem"];
        };
    };
    issueMachineToken: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["TokenRequest"];
            };
        };
        responses: {
            /** @description An access token was issued. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TokenResponse"];
                };
            };
            400: components["responses"]["Problem"];
            401: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    issueOtp: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["IssueOtpRequest"];
            };
        };
        responses: {
            /** @description An OTP was issued (raw value returned once). */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["IssuedOtp"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listPins: {
        parameters: {
            query: {
                identity: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The identity's pins. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PinList"];
                };
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    createPin: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreatePinRequest"];
            };
        };
        responses: {
            /** @description The pin was created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PinResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    revokePin: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                pinId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The pin was revoked (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    issueServiceAccountCredential: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                serviceAccountId: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["IssueServiceAccountCredentialRequest"];
            };
        };
        responses: {
            /** @description The credential was issued. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ServiceAccountCredentialResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    revokeServiceAccountCredential: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                serviceAccountId: string;
                credentialId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The credential was revoked (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    beginDeviceFlow: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["BeginDeviceFlowRequest"];
            };
        };
        responses: {
            /** @description A device flow was started. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeviceFlowResource"];
                };
            };
            400: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    pollDeviceFlow: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PollDeviceFlowRequest"];
            };
        };
        responses: {
            /** @description The current device-flow status. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeviceFlowStatus"];
                };
            };
            400: components["responses"]["Problem"];
            429: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listLocks: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The active locks. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["LockList"];
                };
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    createLock: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateLockRequest"];
            };
        };
        responses: {
            /** @description The lock was created and pushed. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["LockResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    releaseLock: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                lockId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The lock was released (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listJoinTokens: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The active join tokens (metadata only, never the raw token). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JoinTokenList"];
                };
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    issueJoinToken: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["IssueJoinTokenRequest"];
            };
        };
        responses: {
            /** @description A join token was issued (raw value returned once). */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["IssuedJoinToken"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    revokeJoinToken: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                joinTokenId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The join token was revoked (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listNodes: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The enrolled nodes. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["NodeList"];
                };
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    registerNode: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RegisterNodeRequest"];
            };
        };
        responses: {
            /** @description The node was registered. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["NodeResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getNode: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                nodeId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The node. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["NodeResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    removeNode: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                nodeId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The node was removed (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    quarantineNode: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                nodeId: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["QuarantineNodeRequest"];
            };
        };
        responses: {
            /** @description The node was quarantined and the lock pushed. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["NodeResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    releaseQuarantine: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                nodeId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The node was released from quarantine. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["NodeResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listJitRequests: {
        parameters: {
            query?: {
                state?: string;
                requester?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The JIT requests. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JitRequestList"];
                };
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    submitJitRequest: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["JitRequestSubmission"];
            };
        };
        responses: {
            /** @description The JIT request was submitted. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JitRequestResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getJitRequest: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                jitRequestId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The JIT request. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JitRequestResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    approveJitRequest: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                jitRequestId: string;
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["JitDecisionRequest"];
            };
        };
        responses: {
            /** @description The updated JIT request. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JitRequestResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    denyJitRequest: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                jitRequestId: string;
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["JitDecisionRequest"];
            };
        };
        responses: {
            /** @description The denied JIT request. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JitRequestResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    revokeJitRequest: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                jitRequestId: string;
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["JitDecisionRequest"];
            };
        };
        responses: {
            /** @description The revoked JIT request. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JitRequestResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listBreakglassCredentials: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The break-glass credentials. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BreakglassCredentialList"];
                };
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    registerBreakglassCredential: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RegisterBreakglassCredentialRequest"];
            };
        };
        responses: {
            /** @description The credential was registered. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BreakglassCredentialResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    revokeBreakglassCredential: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                credentialId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The credential was revoked (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listBreakglassOfflineCodes: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The offline-code metadata. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BreakglassOfflineCodeList"];
                };
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    issueBreakglassOfflineCodes: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["IssueBreakglassOfflineCodesRequest"];
            };
        };
        responses: {
            /** @description The codes were issued (raw values returned once). */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["IssuedBreakglassOfflineCodes"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listBreakglassActivations: {
        parameters: {
            query?: {
                reviewStatus?: "pending" | "reviewed";
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The break-glass activations. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BreakglassActivationList"];
                };
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    reviewBreakglassActivation: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                activationId: string;
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["ReviewBreakglassActivationRequest"];
            };
        };
        responses: {
            /** @description The reviewed activation. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BreakglassActivationResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listRules: {
        parameters: {
            query?: {
                /**
                 * @description Opaque forward-only pagination cursor from a previous page's `nextCursor`.
                 *     Omit for the first page. An unrecognised cursor is a `400`.
                 */
                cursor?: components["parameters"]["Cursor"];
                /** @description Maximum items to return in one page (the server clamps to its maximum). */
                limit?: components["parameters"]["Limit"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of rules. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RulePage"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    createRule: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional client-generated idempotency key scoping a mutating request. A
                 *     retry with the same key, method, and path returns the original response and
                 *     never repeats the side effect; the same key with a different request body is
                 *     a `422`. Keys are retained for a bounded TTL.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateRuleRequest"];
            };
        };
        responses: {
            /** @description The rule was created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RuleResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getRule: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                ruleId: components["parameters"]["RuleId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The rule. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RuleResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    updateRule: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                ruleId: components["parameters"]["RuleId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateRuleRequest"];
            };
        };
        responses: {
            /** @description The updated rule. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RuleResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    deleteRule: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                ruleId: components["parameters"]["RuleId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The rule was deleted (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listRoles: {
        parameters: {
            query?: {
                /**
                 * @description Opaque forward-only pagination cursor from a previous page's `nextCursor`.
                 *     Omit for the first page. An unrecognised cursor is a `400`.
                 */
                cursor?: components["parameters"]["Cursor"];
                /** @description Maximum items to return in one page (the server clamps to its maximum). */
                limit?: components["parameters"]["Limit"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of roles. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RolePage"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    createRole: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional client-generated idempotency key scoping a mutating request. A
                 *     retry with the same key, method, and path returns the original response and
                 *     never repeats the side effect; the same key with a different request body is
                 *     a `422`. Keys are retained for a bounded TTL.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateRoleRequest"];
            };
        };
        responses: {
            /** @description The role was created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RoleResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getRole: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                roleId: components["parameters"]["RoleId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The role. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RoleResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    updateRole: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                roleId: components["parameters"]["RoleId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateRoleRequest"];
            };
        };
        responses: {
            /** @description The updated role. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RoleResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    deleteRole: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                roleId: components["parameters"]["RoleId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The role was deleted (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listRoleBindings: {
        parameters: {
            query?: {
                /**
                 * @description Opaque forward-only pagination cursor from a previous page's `nextCursor`.
                 *     Omit for the first page. An unrecognised cursor is a `400`.
                 */
                cursor?: components["parameters"]["Cursor"];
                /** @description Maximum items to return in one page (the server clamps to its maximum). */
                limit?: components["parameters"]["Limit"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of role bindings. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RoleBindingPage"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    createRoleBinding: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional client-generated idempotency key scoping a mutating request. A
                 *     retry with the same key, method, and path returns the original response and
                 *     never repeats the side effect; the same key with a different request body is
                 *     a `422`. Keys are retained for a bounded TTL.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateRoleBindingRequest"];
            };
        };
        responses: {
            /** @description The role binding was created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RoleBindingResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getRoleBinding: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                bindingId: components["parameters"]["BindingId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The role binding. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RoleBindingResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    updateRoleBinding: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                bindingId: components["parameters"]["BindingId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateRoleBindingRequest"];
            };
        };
        responses: {
            /** @description The updated role binding. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RoleBindingResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    deleteRoleBinding: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                bindingId: components["parameters"]["BindingId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The role binding was deleted (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listCas: {
        parameters: {
            query?: {
                /**
                 * @description Opaque forward-only pagination cursor from a previous page's `nextCursor`.
                 *     Omit for the first page. An unrecognised cursor is a `400`.
                 */
                cursor?: components["parameters"]["Cursor"];
                /** @description Maximum items to return in one page (the server clamps to its maximum). */
                limit?: components["parameters"]["Limit"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of CA configurations (public/config only). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CaPage"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    createCa: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional client-generated idempotency key scoping a mutating request. A
                 *     retry with the same key, method, and path returns the original response and
                 *     never repeats the side effect; the same key with a different request body is
                 *     a `422`. Keys are retained for a bounded TTL.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateCaRequest"];
            };
        };
        responses: {
            /** @description The CA configuration was created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CaResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getCa: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                caId: components["parameters"]["CaId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The CA configuration. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CaResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    updateCa: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                caId: components["parameters"]["CaId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateCaRequest"];
            };
        };
        responses: {
            /** @description The updated CA configuration. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CaResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    deleteCa: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                caId: components["parameters"]["CaId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The CA configuration was deleted (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    rotateCa: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional client-generated idempotency key scoping a mutating request. A
                 *     retry with the same key, method, and path returns the original response and
                 *     never repeats the side effect; the same key with a different request body is
                 *     a `422`. Keys are retained for a bounded TTL.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path: {
                caId: components["parameters"]["CaId"];
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["RotateCaRequest"];
            };
        };
        responses: {
            /** @description Rotation completed; the new active CA configuration. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CaResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getMtlsTrustAnchor: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The active internal mTLS CA certificate (public trust anchor). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MtlsTrustAnchor"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getCaPublicKey: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Which SSH CA to address. The closed set is the three SSH kinds; the internal mTLS CA is not a member and any other value is a `400`. */
                caKind: components["parameters"]["CaKindPath"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The active CA's public key (public material only). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CaPublicKey"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listGatewayEnrollmentTokens: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The active enrollment tokens (metadata only, never the raw token). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GatewayEnrollmentTokenList"];
                };
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    issueGatewayEnrollmentToken: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["IssueGatewayEnrollmentTokenRequest"];
            };
        };
        responses: {
            /** @description An enrollment token was issued (raw value returned once). */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["IssuedGatewayEnrollmentToken"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    revokeGatewayEnrollmentToken: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                gatewayEnrollmentTokenId: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The enrollment token was revoked (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listGateways: {
        parameters: {
            query?: {
                /**
                 * @description Opaque forward-only pagination cursor from a previous page's `nextCursor`.
                 *     Omit for the first page. An unrecognised cursor is a `400`.
                 */
                cursor?: components["parameters"]["Cursor"];
                /** @description Maximum items to return in one page (the server clamps to its maximum). */
                limit?: components["parameters"]["Limit"];
                /** @description Filter to one Gateway name. */
                name?: string;
                /** @description Filter by identity status. */
                status?: "active" | "locked" | "revoked";
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of Gateway identities (metadata only). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GatewayPage"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getGateway: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                gatewayId: components["parameters"]["GatewayId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The Gateway identity (metadata only). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GatewayResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    removeGateway: {
        parameters: {
            query?: {
                /** @description Remove the identity even while it owns node control channels. Use only when the Gateway is known not to be coming back. */
                force?: boolean;
            };
            header?: never;
            path: {
                gatewayId: components["parameters"]["GatewayId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The Gateway identity was removed (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listServiceAccounts: {
        parameters: {
            query?: {
                /**
                 * @description Opaque forward-only pagination cursor from a previous page's `nextCursor`.
                 *     Omit for the first page. An unrecognised cursor is a `400`.
                 */
                cursor?: components["parameters"]["Cursor"];
                /** @description Maximum items to return in one page (the server clamps to its maximum). */
                limit?: components["parameters"]["Limit"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of service accounts. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ServiceAccountPage"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    createServiceAccount: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional client-generated idempotency key scoping a mutating request. A
                 *     retry with the same key, method, and path returns the original response and
                 *     never repeats the side effect; the same key with a different request body is
                 *     a `422`. Keys are retained for a bounded TTL.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateServiceAccountRequest"];
            };
        };
        responses: {
            /** @description The service account was created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ServiceAccountResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getServiceAccount: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                serviceAccountId: components["parameters"]["ServiceAccountId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The service account. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ServiceAccountResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    updateServiceAccount: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                serviceAccountId: components["parameters"]["ServiceAccountId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateServiceAccountRequest"];
            };
        };
        responses: {
            /** @description The updated service account. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ServiceAccountResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    deleteServiceAccount: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                serviceAccountId: components["parameters"]["ServiceAccountId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The service account was deleted (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listNodePolicies: {
        parameters: {
            query?: {
                /**
                 * @description Opaque forward-only pagination cursor from a previous page's `nextCursor`.
                 *     Omit for the first page. An unrecognised cursor is a `400`.
                 */
                cursor?: components["parameters"]["Cursor"];
                /** @description Maximum items to return in one page (the server clamps to its maximum). */
                limit?: components["parameters"]["Limit"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of node policies. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["NodePolicyPage"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    createNodePolicy: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional client-generated idempotency key scoping a mutating request. A
                 *     retry with the same key, method, and path returns the original response and
                 *     never repeats the side effect; the same key with a different request body is
                 *     a `422`. Keys are retained for a bounded TTL.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateNodePolicyRequest"];
            };
        };
        responses: {
            /** @description The node policy was created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["NodePolicyResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getNodePolicy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                nodePolicyId: components["parameters"]["NodePolicyId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The node policy. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["NodePolicyResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    updateNodePolicy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                nodePolicyId: components["parameters"]["NodePolicyId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateNodePolicyRequest"];
            };
        };
        responses: {
            /** @description The updated node policy. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["NodePolicyResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    deleteNodePolicy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                nodePolicyId: components["parameters"]["NodePolicyId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The node policy was deleted (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listCapabilityDefs: {
        parameters: {
            query?: {
                /**
                 * @description Opaque forward-only pagination cursor from a previous page's `nextCursor`.
                 *     Omit for the first page. An unrecognised cursor is a `400`.
                 */
                cursor?: components["parameters"]["Cursor"];
                /** @description Maximum items to return in one page (the server clamps to its maximum). */
                limit?: components["parameters"]["Limit"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of capability definitions. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CapabilityDefPage"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    createCapabilityDef: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional client-generated idempotency key scoping a mutating request. A
                 *     retry with the same key, method, and path returns the original response and
                 *     never repeats the side effect; the same key with a different request body is
                 *     a `422`. Keys are retained for a bounded TTL.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateCapabilityDefRequest"];
            };
        };
        responses: {
            /** @description The capability definition was created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CapabilityDefResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getCapabilityDef: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                capabilityDefId: components["parameters"]["CapabilityDefId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The capability definition. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CapabilityDefResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    updateCapabilityDef: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                capabilityDefId: components["parameters"]["CapabilityDefId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateCapabilityDefRequest"];
            };
        };
        responses: {
            /** @description The updated capability definition. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CapabilityDefResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    deleteCapabilityDef: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                capabilityDefId: components["parameters"]["CapabilityDefId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The capability definition was deleted (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listJitPolicies: {
        parameters: {
            query?: {
                /**
                 * @description Opaque forward-only pagination cursor from a previous page's `nextCursor`.
                 *     Omit for the first page. An unrecognised cursor is a `400`.
                 */
                cursor?: components["parameters"]["Cursor"];
                /** @description Maximum items to return in one page (the server clamps to its maximum). */
                limit?: components["parameters"]["Limit"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of JIT policies. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JitPolicyPage"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    createJitPolicy: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional client-generated idempotency key scoping a mutating request. A
                 *     retry with the same key, method, and path returns the original response and
                 *     never repeats the side effect; the same key with a different request body is
                 *     a `422`. Keys are retained for a bounded TTL.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateJitPolicyRequest"];
            };
        };
        responses: {
            /** @description The JIT policy was created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JitPolicyResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getJitPolicy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                jitPolicyId: components["parameters"]["JitPolicyId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The JIT policy. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JitPolicyResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    updateJitPolicy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                jitPolicyId: components["parameters"]["JitPolicyId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateJitPolicyRequest"];
            };
        };
        responses: {
            /** @description The updated JIT policy. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JitPolicyResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    deleteJitPolicy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                jitPolicyId: components["parameters"]["JitPolicyId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The JIT policy was deleted (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listBreakglassPolicies: {
        parameters: {
            query?: {
                /**
                 * @description Opaque forward-only pagination cursor from a previous page's `nextCursor`.
                 *     Omit for the first page. An unrecognised cursor is a `400`.
                 */
                cursor?: components["parameters"]["Cursor"];
                /** @description Maximum items to return in one page (the server clamps to its maximum). */
                limit?: components["parameters"]["Limit"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of break-glass policies. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BreakglassPolicyPage"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    createBreakglassPolicy: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional client-generated idempotency key scoping a mutating request. A
                 *     retry with the same key, method, and path returns the original response and
                 *     never repeats the side effect; the same key with a different request body is
                 *     a `422`. Keys are retained for a bounded TTL.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateBreakglassPolicyRequest"];
            };
        };
        responses: {
            /** @description The break-glass policy was created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BreakglassPolicyResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getBreakglassPolicy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                breakglassPolicyId: components["parameters"]["BreakglassPolicyId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The break-glass policy. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BreakglassPolicyResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    updateBreakglassPolicy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                breakglassPolicyId: components["parameters"]["BreakglassPolicyId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateBreakglassPolicyRequest"];
            };
        };
        responses: {
            /** @description The updated break-glass policy. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BreakglassPolicyResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    deleteBreakglassPolicy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                breakglassPolicyId: components["parameters"]["BreakglassPolicyId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The break-glass policy was deleted (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listSessionLimitPolicies: {
        parameters: {
            query?: {
                /**
                 * @description Opaque forward-only pagination cursor from a previous page's `nextCursor`.
                 *     Omit for the first page. An unrecognised cursor is a `400`.
                 */
                cursor?: components["parameters"]["Cursor"];
                /** @description Maximum items to return in one page (the server clamps to its maximum). */
                limit?: components["parameters"]["Limit"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of session-limit policies. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SessionLimitPolicyPage"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    createSessionLimitPolicy: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional client-generated idempotency key scoping a mutating request. A
                 *     retry with the same key, method, and path returns the original response and
                 *     never repeats the side effect; the same key with a different request body is
                 *     a `422`. Keys are retained for a bounded TTL.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateSessionLimitPolicyRequest"];
            };
        };
        responses: {
            /** @description The session-limit policy was created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SessionLimitPolicyResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getSessionLimitPolicy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                sessionLimitPolicyId: components["parameters"]["SessionLimitPolicyId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The session-limit policy. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SessionLimitPolicyResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    updateSessionLimitPolicy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                sessionLimitPolicyId: components["parameters"]["SessionLimitPolicyId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateSessionLimitPolicyRequest"];
            };
        };
        responses: {
            /** @description The updated session-limit policy. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SessionLimitPolicyResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    deleteSessionLimitPolicy: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                sessionLimitPolicyId: components["parameters"]["SessionLimitPolicyId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The session-limit policy was deleted (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getOperatorSettings: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The cluster operator settings. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OperatorSettings"];
                };
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    updateOperatorSettings: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateOperatorSettingsRequest"];
            };
        };
        responses: {
            /** @description The updated operator settings. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OperatorSettings"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getRecordingCustomerKey: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The customer recording key (public material only), or an unprovisioned result. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RecordingCustomerKey"];
                };
            };
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    setRecordingCustomerKey: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SetRecordingCustomerKeyRequest"];
            };
        };
        responses: {
            /** @description The provisioned or rotated key (public material only). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RecordingCustomerKey"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            422: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listSessions: {
        parameters: {
            query?: {
                /**
                 * @description Opaque forward-only pagination cursor from a previous page's `nextCursor`.
                 *     Omit for the first page. An unrecognised cursor is a `400`.
                 */
                cursor?: components["parameters"]["Cursor"];
                /** @description Maximum items to return in one page (the server clamps to its maximum). */
                limit?: components["parameters"]["Limit"];
                /** @description Filter to one subject identity. */
                identity?: string;
                /** @description Filter to one node id. */
                nodeId?: string;
                /** @description Filter to one access model. */
                accessModel?: "standing" | "jit" | "breakglass";
                /** @description When true, return only sessions that have not ended. */
                activeOnly?: boolean;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of sessions. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SessionPage"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getSession: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                sessionId: components["parameters"]["SessionId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The session. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SessionResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    terminateSession: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional client-generated idempotency key scoping a mutating request. A
                 *     retry with the same key, method, and path returns the original response and
                 *     never repeats the side effect; the same key with a different request body is
                 *     a `422`. Keys are retained for a bounded TTL.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path: {
                sessionId: components["parameters"]["SessionId"];
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["TerminateSessionRequest"];
            };
        };
        responses: {
            /** @description The teardown lock was pushed. */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SessionResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listSessionLeases: {
        parameters: {
            query?: {
                /**
                 * @description Opaque forward-only pagination cursor from a previous page's `nextCursor`.
                 *     Omit for the first page. An unrecognised cursor is a `400`.
                 */
                cursor?: components["parameters"]["Cursor"];
                /** @description Maximum items to return in one page (the server clamps to its maximum). */
                limit?: components["parameters"]["Limit"];
                /** @description Filter to one subject identity — the unit the cap is enforced per. */
                identity?: string;
                /** @description When true, return only leases that still count toward the cap — unreleased, and unexpired or without an expiry. This is exactly the predicate the cap evaluates. */
                activeOnly?: boolean;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of concurrency leases. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SessionLeasePage"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getSessionLease: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                sessionLeaseId: components["parameters"]["SessionLeaseId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The concurrency lease. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SessionLeaseResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    releaseSessionLease: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional client-generated idempotency key scoping a mutating request. A
                 *     retry with the same key, method, and path returns the original response and
                 *     never repeats the side effect; the same key with a different request body is
                 *     a `422`. Keys are retained for a bounded TTL.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path: {
                sessionLeaseId: components["parameters"]["SessionLeaseId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ReleaseSessionLeaseRequest"];
            };
        };
        responses: {
            /** @description The released lease. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SessionLeaseResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    listRecordings: {
        parameters: {
            query?: {
                /**
                 * @description Opaque forward-only pagination cursor from a previous page's `nextCursor`.
                 *     Omit for the first page. An unrecognised cursor is a `400`.
                 */
                cursor?: components["parameters"]["Cursor"];
                /** @description Maximum items to return in one page (the server clamps to its maximum). */
                limit?: components["parameters"]["Limit"];
                /** @description Filter to one session id. */
                sessionId?: string;
                /** @description Filter to one subject identity. */
                identity?: string;
                /** @description Filter to one node id. */
                nodeId?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of recording metadata. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RecordingPage"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getRecording: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                recordingId: components["parameters"]["RecordingId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The recording metadata. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RecordingResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    deleteRecording: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional client-generated idempotency key scoping a mutating request. A
                 *     retry with the same key, method, and path returns the original response and
                 *     never repeats the side effect; the same key with a different request body is
                 *     a `422`. Keys are retained for a bounded TTL.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path: {
                recordingId: components["parameters"]["RecordingId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The recording object was governance-deleted (idempotent). */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    replayRecording: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional client-generated idempotency key scoping a mutating request. A
                 *     retry with the same key, method, and path returns the original response and
                 *     never repeats the side effect; the same key with a different request body is
                 *     a `422`. Keys are retained for a bounded TTL.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path: {
                recordingId: components["parameters"]["RecordingId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A short-lived signed URL for replay. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SignedUrl"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    exportRecording: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional client-generated idempotency key scoping a mutating request. A
                 *     retry with the same key, method, and path returns the original response and
                 *     never repeats the side effect; the same key with a different request body is
                 *     a `422`. Keys are retained for a bounded TTL.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path: {
                recordingId: components["parameters"]["RecordingId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A short-lived signed URL for export. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SignedUrl"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            409: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    setRecordingLegalHold: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional client-generated idempotency key scoping a mutating request. A
                 *     retry with the same key, method, and path returns the original response and
                 *     never repeats the side effect; the same key with a different request body is
                 *     a `422`. Keys are retained for a bounded TTL.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path: {
                recordingId: components["parameters"]["RecordingId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["LegalHoldRequest"];
            };
        };
        responses: {
            /** @description The updated recording metadata. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RecordingResource"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    searchAuditEvents: {
        parameters: {
            query?: {
                /**
                 * @description Opaque forward-only pagination cursor from a previous page's `nextCursor`.
                 *     Omit for the first page. An unrecognised cursor is a `400`.
                 */
                cursor?: components["parameters"]["Cursor"];
                /** @description Maximum items to return in one page (the server clamps to its maximum). */
                limit?: components["parameters"]["Limit"];
                /** @description Filter by acting identity. */
                actor?: string;
                /** @description Filter by the subject the action targeted. */
                subject?: string;
                /** @description Filter by action name (e.g. `lock.create`). */
                action?: string;
                /** @description Filter by outcome. */
                outcome?: string;
                /** @description Filter to one session id. */
                sessionId?: string;
                /** @description Filter to one node id. */
                nodeId?: string;
                /** @description Filter by source IP. */
                sourceIp?: string;
                /** @description Inclusive lower bound on occurrence time (RFC 3339). */
                from?: string;
                /** @description Exclusive upper bound on occurrence time (RFC 3339). */
                to?: string;
                /** @description Filter to events whose capability set includes this capability (FR-AUD-8). */
                capability?: components["schemas"]["Capability"];
                /** @description Filter by the session's access model (FR-AUD-8). */
                accessModel?: components["schemas"]["AccessModel"];
                /**
                 * @description Filter by a snapshot node label as `key=value` (FR-AUD-8). Repeatable;
                 *     all supplied labels must match (AND).
                 */
                nodeLabel?: string[];
                /** @description Filter to one correlation id — the FR-AUD-9 full-path join key. */
                correlationId?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of audit events (newest first). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuditEventPage"];
                };
            };
            400: components["responses"]["Problem"];
            403: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
    getAuditEvent: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                auditEventId: components["parameters"]["AuditEventId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The audit event. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuditEventResource"];
                };
            };
            403: components["responses"]["Problem"];
            404: components["responses"]["Problem"];
            default: components["responses"]["Problem"];
        };
    };
}
