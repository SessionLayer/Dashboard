image:
  repository: ghcr.io/sessionlayer/dashboard
  allowUnconfiguredBuild: true
  tag: "v0.0.2"

# Left empty on purpose: connect-src must collapse to 'self' rather than widen,
# and this is the render that proves it.
csp:
  connectSrc: []
